---
description: afw_compile.h API — compile types, residual check, pool/parent/shared, parser file map
globs: src/afw/compile/**,src/afw/include/afw_common.h
alwaysApply: false
---

# Compile API (`compile/afw_compile.h`)

Public entry for Adaptive syntaxes. Output is an **`afw_value_t`** (usually `compiled_value`; JSON returns the value directly). Eval: `afw-script-eval`. EBNF harvest: `afw-compiler-ebnf`. Bootstrap needs functions registered first: `afw-environment` / `register_core`.

## Entry points

| API | Role |
|-----|------|
| `afw_compile_to_value` | Macro → `…_with_callback` + `residual_check_to_full` |
| `afw_compile_to_value_with_callback` | String and/or octet callback; main compiler |
| `afw_compile_and_evaluate[_type]` | Compile then `afw_value_evaluate` |
| `afw_compile_script` / `_template` | Typed shortcuts |
| `afw_compile_to_object` | Strict JSON → `afw_object_t` (not `compiled_value`) |
| `afw_compile_shared_create` | Share literals across compiles on same pool |

**Pool precedence** (one required): `shared->p` → `parent->p` → `p`.

**Nested compiles / `parent`:** Pass `parent` when the nested compile should share symbols and contextual with the outer compiled value. **Do not** pass `parent` for compile-time substitution (`#{…}`): `afw_compile_parse_CompileTimeSubstitution` uses a nested `afw_compile_to_value_with_callback` with `parent=NULL` and `shared=NULL` so the nested script is isolated (no outer locals). It evaluates the nested script immediately and embeds the result.

## Compile types (`AFW_COMPILE_TYPE_MAP` in `afw_common.h`)

| Type | Parser | Result |
|------|--------|--------|
| `json` / `relaxed_json` | `parse_Json` | Direct value (object/list/scalar) |
| `script` | `parse_Script` | `compiled_value` |
| `template` | `parse_Template` | `compiled_value` |
| `test_script` | `parse_TestScript` | `compiled_value` |
| `regexp` / `xpathExpression` | — | **Throws** “not supported yet” at API |
| `error` | — | Invalid |

Data-type hybrid `compileType` in `_AdaptiveDataTypeGenerate_` maps to these enums → `afw_value_compile()`.

**Stale:** comments, `afw` CLI help text, and `usage.xml` may mention `parenthesized` / `parentheses`. It was never a top-level compile type (only an internal expression production) and is not accepted by the CLI parser. See `afw-command` for the actual `-s` mapping.

## Nesting limits (compile vs evaluate)

Type annotations and destructure patterns share **`AFW_COMPILE_PARSE_NESTING_MAX`** (256). Over that is a syntax error (`Type or pattern nesting is too deep`). Header macros `afw_compile_parse_nesting_enter` / `leave` (local `parser`). This is **compile** recursive descent, not the runtime **evaluation stack** (`evaluation_stack_maximum_count`, default 500; Adaptive backtrace). Different layers: `((((integer))))` never evaluates; `foo(foo(foo(…)))` never hits the type parser.

Parenthesized vs function types parse `( Type )` once (a few tokens of lookahead). Do not restore a full scan-to-matching-`)` at every `(`.

## Residual check

`none` | `to_newline` | `to_full` (default) | `to_close_brace`. Enforced in `afw_compile_lexical.c`.

For `to_close_brace`: (1) residual-after-parse does **not** demand EOF (same as `none` in `afw_compile_check_for_residual`); (2) when `compile_type` is `script`, `afw_compile.c` passes `end_is_close_brace=true` to `parse_Script` so the nested body ends at `}`. Used for `#{…}` nested compiles (and similar braced fragments).

## Contextual

`afw_compile_value_contextual_t`: owning `compiled_value` + offset/size into `full_source`. Nodes and errors should carry it (`afw_compile_create_contextual`, embellish on throw).

## Parser file ownership

| File | Owns |
|------|------|
| `afw_compile.c` | Dispatch by compile_type; type info table |
| `afw_compile_lexical.c` | Tokens, residual, parser create/finish, shared |
| `afw_compile_parse.c` | Contextual, symbols, blocks, parse errors |
| `afw_compile_parse_script.c` | Statements, `TestScript`, control flow |
| `afw_compile_parse_expression.c` | Expressions, lambdas, operators → `call_built_in_function` |
| `afw_compile_parse_value.c` | JSON literals / objects / lists |
| `afw_compile_parse_template.c` | Templates; `#{…}` (compile-time, isolated nested script) / `${…}` (eval-time, outer symbols); TemplateString `` `…` `` (string unescapes + `\#`/`\$` opener-suppress) |
| `afw_compile_parse_assignment_target.c` | Destructuring targets |

**Operator lowering:** expression parser emits calls to env **operator** `function_definition`s — requires `afw_generated_register` then `afw_function_internal_prepare_environment` (see `register_core.c`).

## Decompile / compile round-trip

If the change can break **decompile → compile → decompile** (new statement or value kind, type syntax, `#Name` accept/emit, `compiler_internal` argv shape), prove it:

`d1 = decompile(compile(src)); d2 = decompile(compile(d1)); assert(d1 == d2)`

plus evaluate when the original had a result. Tests: `decompile_accept/`, `decompile_fidelity.as`, or the suite that owns the construct. Inventory: `designs/decompile-compiler-internal-inventory.md`. Known non-round-trip: `#closure_binding`, `#function_thunk`.

Related: `afw-compiler-ebnf`, `afw-script-eval`, `afw-function`, `afw-command` (`-s` mapping).
