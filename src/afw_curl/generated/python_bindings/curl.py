#!/usr/bin/env python3

##
# @file curl.py
# @ingroup afw_bindings_python
# @brief This file contains the Python bindings for the curl category.
#

# Generated by afwdev generate afw_curl

import requests
import json

def curl_version_info(session):
    """
    

    Returns run-time libcurl version info.

    Args:
    Returns:
        dict: Returns an object describing the cURL version information.
    """

    request = session.Request()

    action = {
        "function": "curl_version_info"
    }

    request.add_action(action)

    response = request.perform()
    if response.get('status') == 'error':
        raise Exception(response.get('error'))

    return response['actions'][0]['result']

