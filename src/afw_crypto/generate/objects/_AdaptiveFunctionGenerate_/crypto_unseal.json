{
    "category": "crypto",
    "brief": "Unseal AES-GCM sealed data",
    "description": "Decrypt a sealed value from crypto_seal / crypto_encrypt. sealed may be: (1) an object with iv, tag, and ciphertext as base64Binary/hexBinary or as base64/hex strings; (2) a string of pure JSON with those properties as base64 strings (e.g. after stringify of a JSON-friendly object). Returns plaintext octets. Requires execute access.",
    "functionId": "crypto_unseal",
    "functionLabel": "crypto_unseal",
    "requiresExecuteAccess": true,
    "sideEffects": [
        "Other"
    ],
    "parameters": [
        {
            "name": "key",
            "description": "Key material, CryptoKey, or key reference."
        },
        {
            "name": "sealed",
            "description": "Sealed object or pure JSON string."
        }
    ],
    "returns": {
        "dataType": "base64Binary",
        "description": "Plaintext octets. Use decode_to_string() for UTF-8 text."
    }
}
