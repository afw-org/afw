{
    "allowEntity": false,
    "collectionURIs": [
        "/afw/_AdaptiveCollection_/core"
    ],
    "description": "Each property is a template. Compiled when the containing conf or model is loaded. #{…} runs at that compile (including building a function once); ${…} runs when the qualified variable is accessed; a function returned from the template runs when called.",
    "objectType": "_AdaptiveTemplateProperties_",
    "otherProperties": {
        "dataType": "template"
    }
}