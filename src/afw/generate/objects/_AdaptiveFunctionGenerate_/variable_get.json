{
    "brief": "Get a bound variable value",
    "category": "miscellaneous",
    "description": "Return the value of a bound variable. Optional default applies only when the name is not bound — not when the value is undefined. If unbound and no default is given, the result is undefined. The default is the evaluated value at that moment (identity).",
    "functionId": "variable_get",
    "functionLabel": "variable_get",
    "parameters": [
        {
            "dataType": "string",
            "description": "Name of variable to get. Optionally qualifier::name.",
            "name": "name"
        },
        {
            "description": "Value to return only if the name is not bound. The evaluated value at that moment (identity).",
            "name": "defaultValue",
            "optional": true
        }
    ],
    "returns": {
        "description": "Bound variable value, or default / undefined if unbound."
    }
}
