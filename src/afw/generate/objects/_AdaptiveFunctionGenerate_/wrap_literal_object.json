{
    "brief": "Wrap an evaluated object in a memory look-through face",
    "category": "compiler_internal",
    "description": "Evaluate an object value, create a memory object wrapper (afw_object_create_wrapper_*) over its instance, and return that wrapper as an object value. Local property sets stay on the face; gets look through to the shared base. Intended for compile/runtime isolation of object literals (issue #17); not normal author surface syntax.",
    "functionId": "wrap_literal_object",
    "functionLabel": "wrap_literal_object",
    "parameters": [
        {
            "dataType": "object",
            "description": "Object to evaluate and wrap (typically a constant object literal once the compiler emits isolation).",
            "name": "object",
            "optional": false
        }
    ],
    "pure": false,
    "returns": {
        "dataType": "object",
        "description": "A new memory-wrapper object face over the evaluated base."
    },
    "scriptSupport": true
}
