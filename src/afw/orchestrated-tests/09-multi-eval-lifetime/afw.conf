/* Port of multi-eval-lifetime conf — adapter request handler only. */
[
    {
        type                : "requestHandler",
        uriPrefix           : "/",
        requestHandlerType  : "adapter"
    }
]
