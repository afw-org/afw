/* Catalog firehose — core afw adapter only (gate-like conf, load-like schedule). */
[
    {
        type                : "requestHandler",
        uriPrefix           : "/",
        requestHandlerType  : "adapter"
    }
]
