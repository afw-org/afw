/* Minimal conf for orchestrated smoke: adapter request handler only. */
[
    {
        type                : "requestHandler",
        uriPrefix           : "/",
        requestHandlerType  : "adapter"
    }
]
