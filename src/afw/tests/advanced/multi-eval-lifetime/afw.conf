/* Minimal server: perform/eval only — enough for multi-eval process lifetime. */
[
    {
        type                : "requestHandler",
        uriPrefix           : "/",
        requestHandlerType  : "adapter"
    }
]
