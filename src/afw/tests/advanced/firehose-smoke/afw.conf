/* Minimal conf for short firehose smoke (gate-friendly). */
[
    {
        type                : "requestHandler",
        uriPrefix           : "/",
        requestHandlerType  : "adapter"
    }
]
