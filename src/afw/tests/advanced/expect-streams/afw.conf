/* Minimal conf for orchestrated expect-stdout / expect-stderr leaf. */
[
    {
        type                : "requestHandler",
        uriPrefix           : "/",
        requestHandlerType  : "adapter"
    }
]
