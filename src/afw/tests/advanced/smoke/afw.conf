/* Minimal conf for advanced-test smoke: adapter request handler only. */
[
    {
        type                : "requestHandler",
        uriPrefix           : "/",
        requestHandlerType  : "adapter"
    }
]
