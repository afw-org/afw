[
    {
        type                            : "application",
        applicationId                   : "test-auth-adapter",
        description                     : "testing adapter functions that require authorization (issue #90)",
        authorizationControl            : {
            initialAuthorizationCheck   : "const actionId = current::actionId;\nconst resourceId = current::resourceId;\nconst object = current::object;\nconst func = property_get(object, \"function\");\n\nif (resourceId === \"/afw/_AdaptiveObject_/get-object-query\") {\n    if (actionId === \"query\") {\n        return {\n            decisionId: \"deny\",\n            description: \"query denied for test resource\"\n        };\n    }\n}\n\nreturn { decisionId: \"permit\" };"
        }
    }
]
