#!/usr/bin/env python3

##
# @file indexes.py
# @ingroup afw_bindings_python
# @brief This file contains the Python bindings for the indexes category.
#

# Generated by afwdev generate afw

import requests
import json

def index_create(session, key, adapterId=None, value=None, objectType=None, filter=None, options=None, retroactive=None, test=None):
    """
    Create index definition

    Create an index definition.

    Args:
        adapterId (str): Id of adapter.

        key (str): Name of the property index to be created.

        value (str): Adaptive script (expression-like; must return a value)
        used to calculate the index value(s). While evaluating,
        current::object, current::objectId, current::objectType, and
        current::key are available (issue #54). If omitted, the property named
        by key is indexed.

        objectType (list): Object type id(s) this index may apply to.

        filter (str): Adaptive script that must return a boolean to decide
        whether this index applies to a particular object. Uses the same
        current:: variables as value (issue #54). If omitted, the filter is
        always true.

        options (list): Indexing options.

        retroactive (bool): Retroactively generate indexes for existing
        objects.

        test (bool): Test create (don't actually perform).

    Returns:
        dict: Object response from the indexing process
    """

    request = session.Request()

    action = {
        "function": "index_create",
        "key": key
    }

    if adapterId != None:
        action['adapterId'] = adapterId

    if value != None:
        action['value'] = value

    if objectType != None:
        action['objectType'] = objectType

    if filter != None:
        action['filter'] = filter

    if options != None:
        action['options'] = options

    if retroactive != None:
        action['retroactive'] = retroactive

    if test != None:
        action['test'] = test

    request.add_action(action)

    response = request.perform()
    if response.get('status') == 'error':
        raise Exception(response.get('error'))

    return response['actions'][0]['result']

def index_list(session, adapterId=None, objectType=None):
    """
    List property indexes

    List property indexes

    Args:
        adapterId (str): Id of adapter.

        objectType (str): Id of adaptive object type indexes

    Returns:
        dict: Object response from the index repair process
    """

    request = session.Request()

    action = {
        "function": "index_list"
    }

    if adapterId != None:
        action['adapterId'] = adapterId

    if objectType != None:
        action['objectType'] = objectType

    request.add_action(action)

    response = request.perform()
    if response.get('status') == 'error':
        raise Exception(response.get('error'))

    return response['actions'][0]['result']

def index_remove(session, key, adapterId=None):
    """
    Remove index definition

    Remove an index definition.

    Args:
        adapterId (str): Id of adapter.

        key (str): The index key to be removed.

    Returns:
        dict: Object response from the indexing process
    """

    request = session.Request()

    action = {
        "function": "index_remove",
        "key": key
    }

    if adapterId != None:
        action['adapterId'] = adapterId

    request.add_action(action)

    response = request.perform()
    if response.get('status') == 'error':
        raise Exception(response.get('error'))

    return response['actions'][0]['result']

