// See the 'COPYING' file in the project root for licensing information.
/*
 * Adaptive Data Type double
 *
 * Copyright (c) 2010-2024 Clemson University
 *
 */

/*  ----------------------------- N O T E -------------------------------------
 *
 * This file is generated by "afwdev generate afw".
 *
 * Do not change this file directly or the changes will be lost the next time
 * this file is regenerated.
 *
 * -------------------------------------------------------------------------*/


#ifndef __AFW_DATA_TYPE_DOUBLE_BINDING_H__
#define __AFW_DATA_TYPE_DOUBLE_BINDING_H__


#include "afw_minimal.h"
#include "afw_data_type_typedefs.h"

/**
 * @defgroup afw_c_api_data_type_double double
 * @ingroup afw_c_api_data_types
 *
 * Adaptive Data Type double
 *
 * @{
 */

/**
 * @file afw_data_type_double_binding.h
 * @brief Generated header for adaptive data type `double`.
 */

AFW_BEGIN_DECLARES

/** @brief Quoted data type id for double */
#define AFW_DATA_TYPE_ID_Q_double "double"

/** @brief Unquoted data type id for double */
#define AFW_DATA_TYPE_ID_U_double double

/** @brief String for data type id double */
#define AFW_DATA_TYPE_ID_S_double afw_s_double

/** @brief Quoted c type for data type id double */
#define AFW_DATA_TYPE_CTYPE_Q_double "double"

/** @brief Adaptive string value for "double". */
AFW_DECLARE_CONST_DATA(afw_value_t *)
afw_data_type_double_id_value;

/** @brief Data type struct for double. */
AFW_DECLARE_CONST_DATA(afw_data_type_t *)
afw_data_type_double;

/**
 * @brief Unmanaged evaluated value inf for data type double.
 *
 * Lifetime is the containing pool. get_reference and
 * optional_release throw (scalar, object, array).
 * Scalar get_assignable_value creates a managed holdable
 * in xctx->p. Object/array get_assignable_value: managed
 * occupant dual-face, generic memory bag clone_managed,
 * else hold.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_unmanaged_double_inf;

/**
 * @brief Managed evaluated value inf for data type double.
 *
 * Start-at-1 holdable in xctx->p (caller must release).
 * get_reference / get_assignable_value bump. Last-release
 * drops RC; header leak until alloc-pool free is trusted.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_managed_double_inf;

/**
 * @brief Permanent (life of afw environment) value inf for data type double.
 *
 * Lifetime is the afw environment / static const storage.
 * optional_release is NULL. Scalar get_reference /
 * get_assignable_value are as-is. Object/array get_reference
 * is as-is; get_assignable_value isolates (object_hold /
 * array_hold) so slots do not share immortal bags.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_permanent_double_inf;

/**
 * @brief Compile-unit literal inf for data type double.
 *
 * Header lives in the compiled_value pool. get_reference /
 * get_assignable_value are as-is (no promote). clone copies
 * into dest p / xctx->p so a result can outlive the unit.
 * Compiler-only: afw_compile_literal_double_create().
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_compile_literal_double_inf;

/**
 * @brief Macro to determine if data type is double.
 * @param A_DATA_TYPE to test.
 * @return boolean result.
 */
#define afw_data_type_is_double(A_DATA_TYPE) \
( \
    (A_DATA_TYPE) && \
    (A_DATA_TYPE) == afw_data_type_double \
)

/**
 * @brief True if A_VALUE is an evaluated double value.
 * @param A_VALUE to test.
 * @return boolean result.
 *
 * For evaluated values only. When true, it is safe to cast A_VALUE to
 * `const afw_value_double_t *`.
 * If you want to know if the value will be double when fully
 * evaluated (not necessarily cast-safe yet), use
 * `AFW_VALUE_EVALUATES_TO_DATA_TYPE(A_VALUE, double, xctx)` instead.
 */
#define afw_value_is_double(A_VALUE) \
( \
    (A_VALUE) && \
    (A_VALUE)->inf->is_evaluated_of_data_type == afw_data_type_double\
)

/**
 * @brief True if A_VALUE is an evaluated array of double.
 * @param A_VALUE to test.
 * @return boolean result.
 *
 * When true, A_VALUE is an evaluated array (`const afw_value_array_t *`)
 * whose element data type is double.
 */
#define afw_value_is_array_of_double(A_VALUE) \
( \
    afw_value_is_array(A_VALUE) && \
    afw_array_get_data_type( \
        ((const afw_value_array_t *)(A_VALUE))->internal, \
        xctx) == afw_data_type_double \
)

/**
 * @brief Convert data type double string to double *.
 * @param to_internal place to return result.  Must be sizeof(double).
 * @param from_utf8 to convert.
 * @param p to use for returned internal.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_data_type_double_to_internal(
    double *to_internal,
    const afw_utf8_t *from_utf8,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Convert data type double internal representation to utf-8.
 * @param internal (double) to convert.
 * @param p to use for returned string.
 * @param xctx of caller.
 * @return (const afw_utf8_t *) normalized string representation of value.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_data_type_double_to_utf8(
    double internal,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/** @brief struct for data type double values. */
struct afw_value_double_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal double value. */
    double internal;
};

/** @brief struct for managed data type double values.
 *
 * This is the same as afw_value_double_s with the addition of a
 * reference count. This is intended for internal use only.
 */
struct afw_value_double_managed_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal double value. */
    double internal;

    /** @brief  Reference count for value. */
    afw_size_t reference_count;
};

/**
 * @brief Typesafe cast of data type double.
 * @param value (const afw_value_t *).
 * @return (double)
 */
AFW_DECLARE(double)
afw_value_as_double(
    const afw_value_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Allocate function for data type double value.
 * @param p to use for returned value.
 * @param xctx of caller.
 * @return Allocated afw_value_double_t with unmanaged inf set.
 *
 * Unmanaged: lifetime is pool p; no value refcount.
 * Caller fills internal after allocate.
 */
AFW_DECLARE(afw_value_double_t *)
afw_value_double_allocate(
    const afw_pool_t *p,
    afw_xctx_t *xctx);
#define afw_value_allocate_unmanaged_double afw_value_double_allocate

/**
 * @brief Create function for managed data type double value.
 * @param internal.
 * @param xctx of caller.
 * @return Created const afw_value_t *.
 *
 * Allocates in xctx->p. Starts at reference count 1
 * (caller must release). get_reference /
 * get_assignable_value bump. Last-release drops RC;
 * header leak until alloc-pool free is trusted.
 * Stores internal by value in the header.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_double_create_managed(
    double internal,
    afw_xctx_t *xctx);
#define afw_value_create_managed_double afw_value_double_create_managed

/**
 * @brief Deep clone an evaluated double value unmanaged into p.
 * @param value evaluated double.
 * @param p dest pool.
 * @param xctx of caller.
 * @return unmanaged clone in p, or value if permanent.
 *
 * Copies utf8/memory octets into p. Does not release the source.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_clone_double_unmanaged(
    const afw_value_t *value,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Clone an evaluated double value managed in xctx->p.
 * @param value evaluated double.
 * @param xctx of caller.
 * @return managed value (bump if already managed).
 *
 * Permanents as-is. Does not release the source. No dest p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_clone_double_managed(
    const afw_value_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Create function for unmanaged data type double value.
 * @param internal.
 * @param p to use for returned value.
 * @param xctx of caller.
 * @return Created const afw_value_t *.
 *
 * Allocates in pool p; lifetime is the pool (no value refcount).
 * get_reference / release throw. get_assignable_value
 * creates a managed holdable in xctx->p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_double_create(double internal,
    const afw_pool_t *p, afw_xctx_t *xctx);
#define afw_value_create_unmanaged_double afw_value_double_create

/**
 * @brief Create a compile-unit double literal in p.
 * @param internal.
 * @param p compile pool (compiled_value).
 * @param xctx of caller.
 * @return compile_literal inf; as-is in slots; clone copies.
 *
 * Compiler-only. Do not use for eval temps (those stay
 * unmanaged and promote on get_assignable_value).
 */
AFW_DECLARE(const afw_value_t *)
afw_compile_literal_double_create(double internal,
    const afw_pool_t *p, afw_xctx_t *xctx);

/**
 * @brief Get property function for data type double value.
 * @deprecated
 * @param object of property to get.
 * @param property_name of property to get.
 * @param found is place to return whether property is found.
 * @param xctx of caller.
 * @return double.
 *
 * This is a deprecated function used to get around an exception that
 * was occurring when an object did not have a pool. Use the function
 * without an "_old" in the name for all new code and replace calls in
 * old code when possible.
 *
 */
#define afw_object_old_get_property_as_double( \
    object, property_name, found, xctx) \
afw_object_get_property_as_double_source( \
    object, property_name, found, AFW__FILE_LINE__, \
    ((object)->p ? (object)->p : (xctx)->p), (xctx))

/**
 * @brief Get property function for data type double value.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param found is place to return whether property is found.
 * @param p to use for result if evaluation or conversion is required.
 * @param xctx of caller.
 * @return double.
 */
#define afw_object_get_property_as_double( \
    object, property_name, found, p, xctx) \
afw_object_get_property_as_double_source( \
    object, property_name, found, AFW__FILE_LINE__, p, xctx)

/**
 * @brief Get property function for data type double value.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param found is place to return whether property is found.
 * @param source_z file:line.
 * @param p to use for result if evaluation or conversion is required.
 * @param xctx of caller.
 * @return double.
 */
AFW_DECLARE(double)
afw_object_get_property_as_double_source(
    const afw_object_t *object,
    const afw_value_t *property_name,
    afw_boolean_t *found,
    const afw_utf8_z_t *source_z,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Get next property function for data type double value.
 * @deprecated
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param found is place to return whether property is found.
 * @param xctx of caller.
 * @return double.
 *
 * This is a deprecated function used to get around an exception that
 * was occurring when an object did not have a pool. Use the function
 * without an "_old" in the name for all new code and replace calls in
 * old code when possible.
 *
 */
#define afw_object_old_get_next_property_as_double( \
    object, iterator, property_name, found, xctx) \
afw_object_get_next_property_as_double_source( \
    object, iterator, property_name, found, AFW__FILE_LINE__, \
    ((object)->p ? (object)->p : (xctx)->p), (xctx))

/**
 * @brief Get next property function for data type double value.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param found is place to return whether property is found.
 * @param p to use for result if evaluation or conversion is required.
 * @param xctx of caller.
 * @return double.
 */
#define afw_object_get_next_property_as_double( \
    object, iterator, property_name, found, p, xctx) \
afw_object_get_next_property_as_double_source( \
    object, iterator, property_name, found, AFW__FILE_LINE__, p, xctx)

/**
 * @brief Get property function for data type double value.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param found is place to return whether property is found.
 * @param source_z file:line.
 * @param p to use for result if conversion is required.
 * @param xctx of caller.
 * @return double.
 */
AFW_DECLARE(double)
afw_object_get_next_property_as_double_source(
    const afw_object_t *object,
    const afw_iterator_old_t * *iterator,
    const afw_value_t * *property_name,
    afw_boolean_t *found,
    const afw_utf8_z_t *source_z,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Set property function for data type double values.
 * @param object of property to set.
 * @param property_name of property to set.
 * @param value of value to set.
 * @param xctx of caller.
 *
 * The value will be allocated in the object's pool.
 * Prefer afw_object_set_property(..., afw_v_*, ...) when a
 * static const value (e.g. from afw_strings.h) already
 * exists for that constant.
 *
 */
AFW_DECLARE(void)
afw_object_set_property_as_double(
    const afw_object_t *object,
    const afw_value_t *property_name,
    double internal,
    afw_xctx_t *xctx);

/**
 * @brief Get next value from array of double.
 * @param instance of array.
 * @param iterator.
 * @param found is place to return whether value is found.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (double) or NULL.
 * 
 * Set the iterator to NULL before the first call and anytime
 * you want to start from the first value again.
 */
#define afw_array_of_double_get_next( \
    array, iterator, found, xctx) \
    afw_array_of_double_get_next_source( \
    array, iterator, found, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next value from array of double.
 * @param instance of array.
 * @param iterator.
 * @param found is place to return whether value is found.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (double) or NULL.
 * 
 * Set the iterator to NULL before the first call and anytime
 * you want to start from the first value again.
 */
AFW_DECLARE(double)
afw_array_of_double_get_next_source(
    const afw_array_t *instance,
    const afw_iterator_old_t * *iterator,
    afw_boolean_t *found,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Add value from array of double.
 * @param instance of array.
 * @param value to add.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_double_add(
    const afw_array_t *instance,
    const double *value,
    afw_xctx_t *xctx);

/**
 * @brief Remove value from array of double.
 * @param instance of array.
 * @param value to remove.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_double_remove(
    const afw_array_t *instance,
    const double *value,
    afw_xctx_t *xctx);

/**
 * @brief Public data type double struct instance.
 *
 * Prefer afw_data_type_double when a pointer is enough and you are not
 * initializing static data that must reference the struct object.
 */
AFW_DECLARE_CONST_DATA(afw_data_type_t)
afw_data_type_double_direct;

/**
 * @brief Public data type double inf.
 *
 * Defined with the data type implementation; managed by the generated
 * data type instance.
 */
AFW_DECLARE_CONST_DATA(afw_data_type_inf_t)
afw_data_type_double_inf;

AFW_END_DECLARES

/** @} */


#endif /* __AFW_DATA_TYPE_DOUBLE_BINDING_H__ */
