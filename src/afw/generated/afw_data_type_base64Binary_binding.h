// See the 'COPYING' file in the project root for licensing information.
/*
 * Adaptive Data Type base64Binary
 *
 * Copyright (c) 2010-2024 Clemson University
 *
 */

/*  ----------------------------- N O T E -------------------------------------
 *
 * This file is generated by "afwdev generate afw".
 *
 * Do not change this file directly or the changes will be lost the next time
 * this file is regenerated.
 *
 * -------------------------------------------------------------------------*/


#ifndef __AFW_DATA_TYPE_BASE64BINARY_BINDING_H__
#define __AFW_DATA_TYPE_BASE64BINARY_BINDING_H__


#include "afw_minimal.h"
#include "afw_data_type_typedefs.h"

/**
 * @defgroup afw_c_api_data_type_base64Binary base64Binary
 * @ingroup afw_c_api_data_types
 *
 * Adaptive Data Type base64Binary
 *
 * @{
 */

/**
 * @file afw_data_type_base64Binary_binding.h
 * @brief Generated header for adaptive data type `base64Binary`.
 */

AFW_BEGIN_DECLARES

/** @brief Quoted data type id for base64Binary */
#define AFW_DATA_TYPE_ID_Q_base64Binary "base64Binary"

/** @brief Unquoted data type id for base64Binary */
#define AFW_DATA_TYPE_ID_U_base64Binary base64Binary

/** @brief String for data type id base64Binary */
#define AFW_DATA_TYPE_ID_S_base64Binary afw_s_base64Binary

/** @brief Quoted c type for data type id base64Binary */
#define AFW_DATA_TYPE_CTYPE_Q_base64Binary "afw_memory_t"

/** @brief Adaptive string value for "base64Binary". */
AFW_DECLARE_CONST_DATA(afw_value_t *)
afw_data_type_base64Binary_id_value;

/** @brief Data type struct for base64Binary. */
AFW_DECLARE_CONST_DATA(afw_data_type_t *)
afw_data_type_base64Binary;

/**
 * @brief Unmanaged evaluated value inf for data type base64Binary.
 *
 * Lifetime is the containing pool. get_reference and
 * optional_release throw (scalar, object, array).
 * Scalar get_assignable_value creates a managed holdable
 * in xctx->p. Object/array get_assignable_value: managed
 * occupant dual-face, else clone_managed.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_unmanaged_base64Binary_inf;

/**
 * @brief Managed evaluated value inf for data type base64Binary.
 *
 * Start-at-1 holdable in xctx->p (caller must release).
 * get_reference / get_assignable_value bump. Last-release
 * drops RC; header leak until alloc-pool free is trusted.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_managed_base64Binary_inf;

/**
 * @brief Managed slice value inf for data type base64Binary.
 *
 * Slice of a managed value: get_reference containing at create.
 * Header in xctx->p. Slice starts at 1. get_reference bumps
 * the slice. Last release of the slice releases containing;
 * slice header leak until alloc-pool free is trusted.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_managed_slice_base64Binary_inf;

/**
 * @brief Permanent (life of afw environment) value inf for data type base64Binary.
 *
 * Lifetime is the afw environment / static const storage.
 * optional_release is NULL. Scalar get_reference /
 * get_assignable_value are as-is. Object/array get_reference
 * is as-is; get_assignable_value is a managed wrapper
 * (object) or managed clone (array).
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_permanent_base64Binary_inf;

/**
 * @brief Macro to determine if data type is base64Binary.
 * @param A_DATA_TYPE to test.
 * @return boolean result.
 */
#define afw_data_type_is_base64Binary(A_DATA_TYPE) \
( \
    (A_DATA_TYPE) && \
    (A_DATA_TYPE) == afw_data_type_base64Binary \
)

/**
 * @brief True if A_VALUE is an evaluated base64Binary value.
 * @param A_VALUE to test.
 * @return boolean result.
 *
 * For evaluated values only. When true, it is safe to cast A_VALUE to
 * `const afw_value_base64Binary_t *`.
 * If you want to know if the value will be base64Binary when fully
 * evaluated (not necessarily cast-safe yet), use
 * `AFW_VALUE_EVALUATES_TO_DATA_TYPE(A_VALUE, base64Binary, xctx)` instead.
 */
#define afw_value_is_base64Binary(A_VALUE) \
( \
    (A_VALUE) && \
    (A_VALUE)->inf->is_evaluated_of_data_type == afw_data_type_base64Binary\
)

/**
 * @brief True if A_VALUE is an evaluated array of base64Binary.
 * @param A_VALUE to test.
 * @return boolean result.
 *
 * When true, A_VALUE is an evaluated array (`const afw_value_array_t *`)
 * whose element data type is base64Binary.
 */
#define afw_value_is_array_of_base64Binary(A_VALUE) \
( \
    afw_value_is_array(A_VALUE) && \
    afw_array_get_data_type( \
        ((const afw_value_array_t *)(A_VALUE))->internal, \
        xctx) == afw_data_type_base64Binary \
)

/**
 * @brief Convert data type base64Binary string to afw_memory_t *.
 * @param to_internal place to return result.  Must be sizeof(afw_memory_t).
 * @param from_utf8 to convert.
 * @param p to use for returned internal.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_data_type_base64Binary_to_internal(
    afw_memory_t *to_internal,
    const afw_utf8_t *from_utf8,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Convert data type base64Binary internal representation to utf-8.
 * @param internal (const afw_memory_t *) to convert.
 * @param p to use for returned string.
 * @param xctx of caller.
 * @return (const afw_utf8_t *) normalized string representation of value.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_data_type_base64Binary_to_utf8(
    const afw_memory_t * internal,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/** @brief struct for data type base64Binary values. */
struct afw_value_base64Binary_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal afw_memory_t value. */
    afw_memory_t internal;
};

/** @brief struct for managed data type base64Binary values.
 *
 * This is the same as afw_value_base64Binary_s with the addition of a
 * reference count. This is intended for internal use only.
 */
struct afw_value_base64Binary_managed_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal afw_memory_t value. */
    afw_memory_t internal;

    /** @brief  Reference count for value. */
    afw_size_t reference_count;
};

/** @brief struct for managed slice data type base64Binary values.
 *
 * A view into a containing managed value. Holds that value.
 */
struct afw_value_base64Binary_managed_slice_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal afw_memory_t view (pointer into containing). */
    afw_memory_t internal;

    /** @brief  Containing managed value that owns the buffer. */
    const afw_value_base64Binary_managed_t *containing_value;

    /** @brief  Reference count for this slice. */
    afw_size_t reference_count;
};

/**
 * @brief Typesafe cast to evaluated base64Binary value.
 * @param value (const afw_value_t *). Evaluated if needed.
 * @return (const afw_value_base64Binary_t *)
 *
 * Throws if missing or wrong type. Use ->internal for the C
 * payload, or afw_value_as_base64Binary_internal().
 */
AFW_DECLARE(const afw_value_base64Binary_t *)
afw_value_as_base64Binary(
    const afw_value_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Typesafe peel of data type base64Binary internal.
 * @param value (const afw_value_t *).
 * @return (const afw_memory_t *)
 */
AFW_DECLARE(const afw_memory_t *)
afw_value_as_base64Binary_internal(
    const afw_value_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Allocate function for data type base64Binary value.
 * @param p to use for returned value.
 * @param xctx of caller.
 * @return Allocated afw_value_base64Binary_t with unmanaged inf set.
 *
 * Unmanaged: lifetime is pool p; no value refcount.
 * Caller fills internal after allocate.
 */
AFW_DECLARE(afw_value_base64Binary_t *)
afw_value_base64Binary_allocate(
    const afw_pool_t *p,
    afw_xctx_t *xctx);
#define afw_value_allocate_unmanaged_base64Binary afw_value_base64Binary_allocate

/**
 * @brief Create function for managed data type base64Binary value.
 * @param internal.
 * @param xctx of caller.
 * @return Created const afw_value_t *.
 *
 * Allocates in xctx->p. Starts at reference count 1
 * (caller must release). get_reference /
 * get_assignable_value bump. Last-release drops RC;
 * header leak until alloc-pool free is trusted.
 * Copies bytes into storage following the header (value owns them).
 */
AFW_DECLARE(const afw_value_t *)
afw_value_base64Binary_create_managed(
    const afw_memory_t * internal,
    afw_xctx_t *xctx);
#define afw_value_create_managed_base64Binary afw_value_base64Binary_create_managed

/**
 * @brief Deep clone an evaluated base64Binary value unmanaged into p.
 * @param value evaluated base64Binary.
 * @param p dest pool.
 * @param xctx of caller.
 * @return unmanaged clone in p, or value if permanent.
 *
 * Copies utf8/memory octets into p. Does not release the source.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_clone_base64Binary_unmanaged(
    const afw_value_t *value,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Clone an evaluated base64Binary value managed in xctx->p.
 * @param value evaluated base64Binary.
 * @param xctx of caller.
 * @return managed value (bump if already managed).
 *
 * Permanents as-is. Does not release the source. No dest p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_clone_base64Binary_managed(
    const afw_value_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Create a managed slice of a managed data type base64Binary value.
 * @param containing_value managed (or managed_slice) value with cType afw_memory_t.
 * @param offset into containing value's internal.
 * @param size of slice.
 * @param xctx of caller.
 * @return Created const afw_value_t * (managed_slice inf).
 *
 * View of a managed memory value. get_reference on containing. Slice
 * starts at 1 (caller must release). Header allocated in xctx->p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_base64Binary_create_managed_slice(
    const afw_value_t *containing_value,
    afw_size_t offset,
    afw_size_t size,
    afw_xctx_t *xctx);
#define afw_value_create_managed_base64Binary_slice afw_value_base64Binary_create_managed_slice

/**
 * @brief Create function for unmanaged data type base64Binary value.
 * @param internal.
 * @param p to use for returned value.
 * @param xctx of caller.
 * @return Created const afw_value_t *.
 *
 * Allocates in pool p; lifetime is the pool (no value refcount).
 * Copies the utf8/memory header only, not the octets.
 * get_reference / release throw. get_assignable_value
 * creates a managed holdable in xctx->p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_base64Binary_create(const afw_memory_t * internal,
    const afw_pool_t *p, afw_xctx_t *xctx);
#define afw_value_create_unmanaged_base64Binary afw_value_base64Binary_create

/**
 * @brief Get property as base64Binary value.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param xctx of caller.
 * @return (const afw_value_base64Binary_t *) or NULL if missing.
 *
 * Does not evaluate. Throws if present but not base64Binary.
 */
#define afw_object_get_property_as_base64Binary( \
    object, property_name, xctx) \
afw_object_get_property_as_base64Binary_source( \
    object, property_name, AFW__FILE_LINE__, xctx)

/**
 * @brief Get property as base64Binary value.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_value_base64Binary_t *) or NULL if missing.
 */
AFW_DECLARE(const afw_value_base64Binary_t *)
afw_object_get_property_as_base64Binary_source(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Get property function for data type base64Binary internal.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param xctx of caller.
 * @return const afw_memory_t *.
 */
#define afw_object_get_property_as_base64Binary_internal( \
    object, property_name, xctx) \
afw_object_get_property_as_base64Binary_internal_source( \
    object, property_name, AFW__FILE_LINE__, xctx)

/**
 * @brief Get property function for data type base64Binary value.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return const afw_memory_t *.
 */
AFW_DECLARE(const afw_memory_t *)
afw_object_get_property_as_base64Binary_internal_source(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Get next property as base64Binary value.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param xctx of caller.
 * @return (const afw_value_base64Binary_t *) or NULL if no more.
 */
#define afw_object_get_next_property_as_base64Binary( \
    object, iterator, property_name, xctx) \
afw_object_get_next_property_as_base64Binary_source( \
    object, iterator, property_name, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next property as base64Binary value.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_value_base64Binary_t *) or NULL if no more.
 */
AFW_DECLARE(const afw_value_base64Binary_t *)
afw_object_get_next_property_as_base64Binary_source(
    const afw_object_t *object,
    const afw_iterator_old_t * *iterator,
    const afw_value_t * *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Get next property function for data type base64Binary internal.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param xctx of caller.
 * @return const afw_memory_t *.
 */
#define afw_object_get_next_property_as_base64Binary_internal( \
    object, iterator, property_name, xctx) \
afw_object_get_next_property_as_base64Binary_internal_source( \
    object, iterator, property_name, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next property function for data type base64Binary value.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return const afw_memory_t *.
 */
AFW_DECLARE(const afw_memory_t *)
afw_object_get_next_property_as_base64Binary_internal_source(
    const afw_object_t *object,
    const afw_iterator_old_t * *iterator,
    const afw_value_t * *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Set property as base64Binary value.
 * @param object of property to set.
 * @param property_name of property to set.
 * @param value to set.
 * @param xctx of caller.
 *
 * Compile-time type check for const afw_value_base64Binary_t *.
 */
AFW_DECLARE(void)
afw_object_set_property_as_base64Binary(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_value_base64Binary_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Set property as base64Binary internal.
 * @param object of property to set.
 * @param property_name of property to set.
 * @param internal of value to set.
 * @param xctx of caller.
 *
 * The value will be allocated in the object's pool.
 * Prefer afw_object_set_property(..., afw_v_*, ...) when a
 * static const value (e.g. from afw_strings.h) already
 * exists for that constant.
 *
 */
AFW_DECLARE(void)
afw_object_set_property_as_base64Binary_internal(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_memory_t * internal,
    afw_xctx_t *xctx);

/**
 * @brief Get next base64Binary value from array of base64Binary.
 * @param instance of array.
 * @param iterator.
 * @param xctx of caller.
 * @return (const afw_value_base64Binary_t *) or NULL.
 *
 * Set the iterator to NULL before the first call and anytime
 * you want to start from the first value again.
 */
#define afw_array_of_base64Binary_get_next( \
    array, iterator, xctx) \
    afw_array_of_base64Binary_get_next_source( \
    array, iterator, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next base64Binary value from array of base64Binary.
 * @param instance of array.
 * @param iterator.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_value_base64Binary_t *) or NULL.
 */
AFW_DECLARE(const afw_value_base64Binary_t *)
afw_array_of_base64Binary_get_next_source(
    const afw_array_t *instance,
    const afw_iterator_old_t * *iterator,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Get next base64Binary internal from array of base64Binary.
 * @param instance of array.
 * @param iterator.
 * @param xctx of caller.
 * @return (const afw_memory_t *) or NULL.
 */
#define afw_array_of_base64Binary_get_next_internal( \
    array, iterator, xctx) \
    afw_array_of_base64Binary_get_next_internal_source( \
    array, iterator, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next base64Binary internal from array of base64Binary.
 * @param instance of array.
 * @param iterator.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_memory_t *) or NULL.
 */
AFW_DECLARE(const afw_memory_t *)
afw_array_of_base64Binary_get_next_internal_source(
    const afw_array_t *instance,
    const afw_iterator_old_t * *iterator,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Add a base64Binary value to array of base64Binary.
 * @param instance of array.
 * @param value to add.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_base64Binary_add(
    const afw_array_t *instance,
    const afw_value_base64Binary_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Add a base64Binary internal to array of base64Binary.
 * @param instance of array.
 * @param value to add.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_base64Binary_add_internal(
    const afw_array_t *instance,
    const afw_memory_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Remove a base64Binary value from array of base64Binary.
 * @param instance of array.
 * @param value to remove.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_base64Binary_remove(
    const afw_array_t *instance,
    const afw_value_base64Binary_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Remove a base64Binary internal from array of base64Binary.
 * @param instance of array.
 * @param value to remove.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_base64Binary_remove_internal(
    const afw_array_t *instance,
    const afw_memory_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Public data type base64Binary struct instance.
 *
 * Prefer afw_data_type_base64Binary when a pointer is enough and you are not
 * initializing static data that must reference the struct object.
 */
AFW_DECLARE_CONST_DATA(afw_data_type_t)
afw_data_type_base64Binary_direct;

/**
 * @brief Public data type base64Binary inf.
 *
 * Defined with the data type implementation; managed by the generated
 * data type instance.
 */
AFW_DECLARE_CONST_DATA(afw_data_type_inf_t)
afw_data_type_base64Binary_inf;

AFW_END_DECLARES

/** @} */


#endif /* __AFW_DATA_TYPE_BASE64BINARY_BINDING_H__ */
