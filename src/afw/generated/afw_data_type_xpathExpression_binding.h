// See the 'COPYING' file in the project root for licensing information.
/*
 * Adaptive Data Type xpathExpression
 *
 * Copyright (c) 2010-2024 Clemson University
 *
 */

/*  ----------------------------- N O T E -------------------------------------
 *
 * This file is generated by "afwdev generate afw".
 *
 * Do not change this file directly or the changes will be lost the next time
 * this file is regenerated.
 *
 * -------------------------------------------------------------------------*/


#ifndef __AFW_DATA_TYPE_XPATHEXPRESSION_BINDING_H__
#define __AFW_DATA_TYPE_XPATHEXPRESSION_BINDING_H__


#include "afw_minimal.h"
#include "afw_data_type_typedefs.h"

/**
 * @defgroup afw_c_api_data_type_xpathExpression xpathExpression
 * @ingroup afw_c_api_data_types
 *
 * Adaptive Data Type xpathExpression
 *
 * @{
 */

/**
 * @file afw_data_type_xpathExpression_binding.h
 * @brief Generated header for adaptive data type `xpathExpression`.
 */

AFW_BEGIN_DECLARES

/** @brief Quoted data type id for xpathExpression */
#define AFW_DATA_TYPE_ID_Q_xpathExpression "xpathExpression"

/** @brief Unquoted data type id for xpathExpression */
#define AFW_DATA_TYPE_ID_U_xpathExpression xpathExpression

/** @brief String for data type id xpathExpression */
#define AFW_DATA_TYPE_ID_S_xpathExpression afw_s_xpathExpression

/** @brief Quoted c type for data type id xpathExpression */
#define AFW_DATA_TYPE_CTYPE_Q_xpathExpression "afw_utf8_t"

/** @brief Adaptive string value for "xpathExpression". */
AFW_DECLARE_CONST_DATA(afw_value_t *)
afw_data_type_xpathExpression_id_value;

/** @brief Data type struct for xpathExpression. */
AFW_DECLARE_CONST_DATA(afw_data_type_t *)
afw_data_type_xpathExpression;

/**
 * @brief Unmanaged evaluated value inf for data type xpathExpression.
 *
 * Lifetime is the containing pool. get_reference and
 * optional_release throw (scalar, object, array).
 * Scalar get_assignable_value creates a managed holdable
 * in xctx->p. Object/array get_assignable_value: managed
 * occupant dual-face, else clone_managed.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_unmanaged_xpathExpression_inf;

/**
 * @brief Managed evaluated value inf for data type xpathExpression.
 *
 * Start-at-1 holdable in xctx->p (caller must release).
 * get_reference / get_assignable_value bump. Scalar
 * last-release free_memorys the header via xctx->p.
 * Object/array: instance last-release (embedded dual-face
 * has no extra header).
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_managed_xpathExpression_inf;

/**
 * @brief Managed slice value inf for data type xpathExpression.
 *
 * Slice of a managed value: get_reference containing at create.
 * Header in xctx->p. Slice starts at 1. get_reference bumps
 * the slice. Last release of the slice releases containing
 * and free_memorys the slice header via xctx->p.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_managed_slice_xpathExpression_inf;

/**
 * @brief Permanent (life of afw environment) value inf for data type xpathExpression.
 *
 * Lifetime is the afw environment / static const storage.
 * optional_release is NULL. Scalar get_reference /
 * get_assignable_value are as-is. Object/array get_reference
 * is as-is; get_assignable_value is a managed wrapper
 * (object) or managed clone (array).
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_permanent_xpathExpression_inf;

/**
 * @brief Macro to determine if data type is xpathExpression.
 * @param A_DATA_TYPE to test.
 * @return boolean result.
 */
#define afw_data_type_is_xpathExpression(A_DATA_TYPE) \
( \
    (A_DATA_TYPE) && \
    (A_DATA_TYPE) == afw_data_type_xpathExpression \
)

/**
 * @brief True if A_VALUE is an evaluated xpathExpression value.
 * @param A_VALUE to test.
 * @return boolean result.
 *
 * For evaluated values only. When true, it is safe to cast A_VALUE to
 * `const afw_value_xpathExpression_t *`.
 * If you want to know if the value will be xpathExpression when fully
 * evaluated (not necessarily cast-safe yet), use
 * `AFW_VALUE_EVALUATES_TO_DATA_TYPE(A_VALUE, xpathExpression, xctx)` instead.
 */
#define afw_value_is_xpathExpression(A_VALUE) \
( \
    (A_VALUE) && \
    (A_VALUE)->inf->is_evaluated_of_data_type == afw_data_type_xpathExpression\
)

/**
 * @brief True if A_VALUE is an evaluated array of xpathExpression.
 * @param A_VALUE to test.
 * @return boolean result.
 *
 * When true, A_VALUE is an evaluated array (`const afw_value_array_t *`)
 * whose element data type is xpathExpression.
 */
#define afw_value_is_array_of_xpathExpression(A_VALUE) \
( \
    afw_value_is_array(A_VALUE) && \
    afw_array_get_data_type( \
        ((const afw_value_array_t *)(A_VALUE))->internal, \
        xctx) == afw_data_type_xpathExpression \
)

/**
 * @brief Convert data type xpathExpression string to afw_utf8_t *.
 * @param to_internal place to return result.  Must be sizeof(afw_utf8_t).
 * @param from_utf8 to convert.
 * @param p to use for returned internal.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_data_type_xpathExpression_to_internal(
    afw_utf8_t *to_internal,
    const afw_utf8_t *from_utf8,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Convert data type xpathExpression internal representation to utf-8.
 * @param internal (const afw_utf8_t *) to convert.
 * @param p to use for returned string.
 * @param xctx of caller.
 * @return (const afw_utf8_t *) normalized string representation of value.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_data_type_xpathExpression_to_utf8(
    const afw_utf8_t * internal,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/** @brief struct for data type xpathExpression values. */
struct afw_value_xpathExpression_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal afw_utf8_t value. */
    afw_utf8_t internal;
};

/** @brief struct for managed data type xpathExpression values.
 *
 * This is the same as afw_value_xpathExpression_s with the addition of a
 * reference count. This is intended for internal use only.
 */
struct afw_value_xpathExpression_managed_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal afw_utf8_t value. */
    afw_utf8_t internal;

    /** @brief  Reference count for value. */
    afw_size_t reference_count;
};

/** @brief struct for managed slice data type xpathExpression values.
 *
 * A view into a containing managed value. Holds that value.
 */
struct afw_value_xpathExpression_managed_slice_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal afw_utf8_t view (pointer into containing). */
    afw_utf8_t internal;

    /** @brief  Containing managed value that owns the buffer. */
    const afw_value_xpathExpression_managed_t *containing_value;

    /** @brief  Reference count for this slice. */
    afw_size_t reference_count;
};

/**
 * @brief Typesafe cast to evaluated xpathExpression value.
 * @param value (const afw_value_t *). Evaluated if needed.
 * @return (const afw_value_xpathExpression_t *)
 *
 * Throws if missing or wrong type. Use ->internal for the C
 * payload, or afw_value_as_xpathExpression_internal().
 */
AFW_DECLARE(const afw_value_xpathExpression_t *)
afw_value_as_xpathExpression(
    const afw_value_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Typesafe peel of data type xpathExpression internal.
 * @param value (const afw_value_t *).
 * @return (const afw_utf8_t *)
 */
AFW_DECLARE(const afw_utf8_t *)
afw_value_as_xpathExpression_internal(
    const afw_value_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Allocate function for data type xpathExpression value.
 * @param p to use for returned value.
 * @param xctx of caller.
 * @return Allocated afw_value_xpathExpression_t with unmanaged inf set.
 *
 * Unmanaged: lifetime is pool p; no value refcount.
 * Caller fills internal after allocate.
 */
AFW_DECLARE(afw_value_xpathExpression_t *)
afw_value_xpathExpression_allocate(
    const afw_pool_t *p,
    afw_xctx_t *xctx);
#define afw_value_allocate_unmanaged_xpathExpression afw_value_xpathExpression_allocate

/**
 * @brief Create function for managed data type xpathExpression value.
 * @param internal.
 * @param xctx of caller.
 * @return Created const afw_value_t *.
 *
 * Allocates in xctx->p. Starts at reference count 1
 * (caller must release). get_reference /
 * get_assignable_value bump. Last-release
 * free_memorys the header via xctx->p.
 * Copies bytes into storage following the header (value owns them).
 */
AFW_DECLARE(const afw_value_t *)
afw_value_xpathExpression_create_managed(
    const afw_utf8_t * internal,
    afw_xctx_t *xctx);
#define afw_value_create_managed_xpathExpression afw_value_xpathExpression_create_managed

/**
 * @brief Deep clone an evaluated xpathExpression value unmanaged into p.
 * @param value evaluated xpathExpression.
 * @param p dest pool.
 * @param xctx of caller.
 * @return unmanaged clone in p, or value if permanent.
 *
 * Copies utf8/memory octets into p. Does not release the source.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_clone_xpathExpression_unmanaged(
    const afw_value_t *value,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Clone an evaluated xpathExpression value managed in xctx->p.
 * @param value evaluated xpathExpression.
 * @param xctx of caller.
 * @return managed value (bump if already managed).
 *
 * Permanents as-is. Does not release the source. No dest p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_clone_xpathExpression_managed(
    const afw_value_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Create a managed slice of a managed data type xpathExpression value.
 * @param containing_value managed (or managed_slice) value with cType afw_utf8_t.
 * @param offset into containing value's internal.
 * @param len of slice.
 * @param xctx of caller.
 * @return Created const afw_value_t * (managed_slice inf).
 *
 * View of a managed string. get_reference on containing. Slice starts
 * at 1 (caller must release). Header allocated in xctx->p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_xpathExpression_create_managed_slice(
    const afw_value_t *containing_value,
    afw_size_t offset,
    afw_size_t len,
    afw_xctx_t *xctx);
#define afw_value_create_managed_xpathExpression_slice afw_value_xpathExpression_create_managed_slice

/**
 * @brief Create function for unmanaged data type xpathExpression value.
 * @param internal.
 * @param p to use for returned value.
 * @param xctx of caller.
 * @return Created const afw_value_t *.
 *
 * Allocates in pool p; lifetime is the pool (no value refcount).
 * Copies the utf8/memory header only, not the octets.
 * get_reference / release throw. get_assignable_value
 * creates a managed holdable in xctx->p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_xpathExpression_create(const afw_utf8_t * internal,
    const afw_pool_t *p, afw_xctx_t *xctx);
#define afw_value_create_unmanaged_xpathExpression afw_value_xpathExpression_create

/**
 * @brief Get property as xpathExpression value.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param xctx of caller.
 * @return (const afw_value_xpathExpression_t *) or NULL if missing.
 *
 * Does not evaluate. Throws if present but not xpathExpression.
 */
#define afw_object_get_property_as_xpathExpression( \
    object, property_name, xctx) \
afw_object_get_property_as_xpathExpression_source( \
    object, property_name, AFW__FILE_LINE__, xctx)

/**
 * @brief Get property as xpathExpression value.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_value_xpathExpression_t *) or NULL if missing.
 */
AFW_DECLARE(const afw_value_xpathExpression_t *)
afw_object_get_property_as_xpathExpression_source(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Get property as xpathExpression internal.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 */
#define afw_object_get_property_as_xpathExpression_internal( \
    object, property_name, xctx) \
afw_object_get_property_as_xpathExpression_internal_source( \
    object, property_name, AFW__FILE_LINE__, xctx)

/**
 * @brief Get property as xpathExpression internal.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_object_get_property_as_xpathExpression_internal_source(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Get next property as xpathExpression value.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param xctx of caller.
 * @return (const afw_value_xpathExpression_t *) or NULL if no more.
 */
#define afw_object_get_next_property_as_xpathExpression( \
    object, iterator, property_name, xctx) \
afw_object_get_next_property_as_xpathExpression_source( \
    object, iterator, property_name, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next property as xpathExpression value.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_value_xpathExpression_t *) or NULL if no more.
 */
AFW_DECLARE(const afw_value_xpathExpression_t *)
afw_object_get_next_property_as_xpathExpression_source(
    const afw_object_t *object,
    const afw_iterator_old_t * *iterator,
    const afw_value_t * *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Get next property as xpathExpression internal.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 */
#define afw_object_get_next_property_as_xpathExpression_internal( \
    object, iterator, property_name, xctx) \
afw_object_get_next_property_as_xpathExpression_internal_source( \
    object, iterator, property_name, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next property as xpathExpression internal.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_object_get_next_property_as_xpathExpression_internal_source(
    const afw_object_t *object,
    const afw_iterator_old_t * *iterator,
    const afw_value_t * *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Set property as xpathExpression value.
 * @param object of property to set.
 * @param property_name of property to set.
 * @param value to set.
 * @param xctx of caller.
 *
 * Compile-time type check for const afw_value_xpathExpression_t *.
 */
AFW_DECLARE(void)
afw_object_set_property_as_xpathExpression(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_value_xpathExpression_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Set property as xpathExpression internal.
 * @param object of property to set.
 * @param property_name of property to set.
 * @param internal of value to set.
 * @param xctx of caller.
 *
 * The value will be allocated in the object's pool.
 * Prefer afw_object_set_property(..., afw_v_*, ...) when a
 * static const value (e.g. from afw_strings.h) already
 * exists for that constant.
 *
 */
AFW_DECLARE(void)
afw_object_set_property_as_xpathExpression_internal(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_t * internal,
    afw_xctx_t *xctx);

/**
 * @brief Get next xpathExpression value from array of xpathExpression.
 * @param instance of array.
 * @param iterator.
 * @param xctx of caller.
 * @return (const afw_value_xpathExpression_t *) or NULL.
 *
 * Set the iterator to NULL before the first call and anytime
 * you want to start from the first value again.
 */
#define afw_array_of_xpathExpression_get_next( \
    array, iterator, xctx) \
    afw_array_of_xpathExpression_get_next_source( \
    array, iterator, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next xpathExpression value from array of xpathExpression.
 * @param instance of array.
 * @param iterator.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_value_xpathExpression_t *) or NULL.
 */
AFW_DECLARE(const afw_value_xpathExpression_t *)
afw_array_of_xpathExpression_get_next_source(
    const afw_array_t *instance,
    const afw_iterator_old_t * *iterator,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Get next xpathExpression internal from array of xpathExpression.
 * @param instance of array.
 * @param iterator.
 * @param xctx of caller.
 * @return (const afw_utf8_t *) or NULL.
 */
#define afw_array_of_xpathExpression_get_next_internal( \
    array, iterator, xctx) \
    afw_array_of_xpathExpression_get_next_internal_source( \
    array, iterator, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next xpathExpression internal from array of xpathExpression.
 * @param instance of array.
 * @param iterator.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_utf8_t *) or NULL.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_array_of_xpathExpression_get_next_internal_source(
    const afw_array_t *instance,
    const afw_iterator_old_t * *iterator,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Add a xpathExpression value to array of xpathExpression.
 * @param instance of array.
 * @param value to add.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_xpathExpression_add(
    const afw_array_t *instance,
    const afw_value_xpathExpression_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Add a xpathExpression internal to array of xpathExpression.
 * @param instance of array.
 * @param value to add.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_xpathExpression_add_internal(
    const afw_array_t *instance,
    const afw_utf8_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Remove a xpathExpression value from array of xpathExpression.
 * @param instance of array.
 * @param value to remove.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_xpathExpression_remove(
    const afw_array_t *instance,
    const afw_value_xpathExpression_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Remove a xpathExpression internal from array of xpathExpression.
 * @param instance of array.
 * @param value to remove.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_xpathExpression_remove_internal(
    const afw_array_t *instance,
    const afw_utf8_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Public data type xpathExpression struct instance.
 *
 * Prefer afw_data_type_xpathExpression when a pointer is enough and you are not
 * initializing static data that must reference the struct object.
 */
AFW_DECLARE_CONST_DATA(afw_data_type_t)
afw_data_type_xpathExpression_direct;

/**
 * @brief Public data type xpathExpression inf.
 *
 * Defined with the data type implementation; managed by the generated
 * data type instance.
 */
AFW_DECLARE_CONST_DATA(afw_data_type_inf_t)
afw_data_type_xpathExpression_inf;

AFW_END_DECLARES

/** @} */


#endif /* __AFW_DATA_TYPE_XPATHEXPRESSION_BINDING_H__ */
