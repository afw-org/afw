// See the 'COPYING' file in the project root for licensing information.
/*
 * Adaptive Data Type ia5String
 *
 * Copyright (c) 2010-2024 Clemson University
 *
 */

/*  ----------------------------- N O T E -------------------------------------
 *
 * This file is generated by "afwdev generate afw".
 *
 * Do not change this file directly or the changes will be lost the next time
 * this file is regenerated.
 *
 * -------------------------------------------------------------------------*/


#ifndef __AFW_DATA_TYPE_IA5STRING_BINDING_H__
#define __AFW_DATA_TYPE_IA5STRING_BINDING_H__


#include "afw_minimal.h"
#include "afw_data_type_typedefs.h"

/**
 * @defgroup afw_c_api_data_type_ia5String ia5String
 * @ingroup afw_c_api_data_types
 *
 * Adaptive Data Type ia5String
 *
 * @{
 */

/**
 * @file afw_data_type_ia5String_binding.h
 * @brief Generated header for adaptive data type `ia5String`.
 */

AFW_BEGIN_DECLARES

/** @brief Quoted data type id for ia5String */
#define AFW_DATA_TYPE_ID_Q_ia5String "ia5String"

/** @brief Unquoted data type id for ia5String */
#define AFW_DATA_TYPE_ID_U_ia5String ia5String

/** @brief String for data type id ia5String */
#define AFW_DATA_TYPE_ID_S_ia5String afw_s_ia5String

/** @brief Quoted c type for data type id ia5String */
#define AFW_DATA_TYPE_CTYPE_Q_ia5String "afw_utf8_t"

/** @brief Adaptive string value for "ia5String". */
AFW_DECLARE_CONST_DATA(afw_value_t *)
afw_data_type_ia5String_id_value;

/** @brief Data type struct for ia5String. */
AFW_DECLARE_CONST_DATA(afw_data_type_t *)
afw_data_type_ia5String;

/**
 * @brief Unmanaged evaluated value inf for data type ia5String.
 *
 * Lifetime is the containing pool until clone_or_reference.
 * Scalar clone_or_reference creates a managed holdable in p.
 * Object/array clone_or_reference holds a memory face.
 * optional_release is NULL on unmanaged scalars.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_unmanaged_ia5String_inf;

/**
 * @brief Managed evaluated value inf for data type ia5String.
 *
 * Start-at-1 holdable clone in p (must release). clone_or_reference
 * bumps. Last release frees via stored p.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_managed_ia5String_inf;

/**
 * @brief Managed slice value inf for data type ia5String.
 *
 * Slice of a managed value: get_reference containing at create.
 * Slice starts at 1. clone_or_reference bumps the slice. Last
 * release of the slice releases containing and frees the slice.
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_managed_slice_ia5String_inf;

/**
 * @brief Permanent (life of afw environment) value inf for data type ia5String.
 *
 * Lifetime is the afw environment / static const storage. optional_release
 * is NULL. Scalar clone_or_reference is as-is. Object/array
 * clone_or_reference holds a memory face (same as unmanaged).
 */
AFW_DECLARE_CONST_DATA(afw_value_inf_t)
afw_value_permanent_ia5String_inf;

/**
 * @brief Macro to determine if data type is ia5String.
 * @param A_DATA_TYPE to test.
 * @return boolean result.
 */
#define afw_data_type_is_ia5String(A_DATA_TYPE) \
( \
    (A_DATA_TYPE) && \
    (A_DATA_TYPE) == afw_data_type_ia5String \
)

/**
 * @brief True if A_VALUE is an evaluated ia5String value.
 * @param A_VALUE to test.
 * @return boolean result.
 *
 * For evaluated values only. When true, it is safe to cast A_VALUE to
 * `const afw_value_ia5String_t *`.
 * If you want to know if the value will be ia5String when fully
 * evaluated (not necessarily cast-safe yet), use
 * `AFW_VALUE_EVALUATES_TO_DATA_TYPE(A_VALUE, ia5String, xctx)` instead.
 */
#define afw_value_is_ia5String(A_VALUE) \
( \
    (A_VALUE) && \
    (A_VALUE)->inf->is_evaluated_of_data_type == afw_data_type_ia5String\
)

/**
 * @brief True if A_VALUE is an evaluated array of ia5String.
 * @param A_VALUE to test.
 * @return boolean result.
 *
 * When true, A_VALUE is an evaluated array (`const afw_value_array_t *`)
 * whose element data type is ia5String.
 */
#define afw_value_is_array_of_ia5String(A_VALUE) \
( \
    afw_value_is_array(A_VALUE) && \
    afw_array_get_data_type( \
        ((const afw_value_array_t *)(A_VALUE))->internal, \
        xctx) == afw_data_type_ia5String \
)

/**
 * @brief Convert data type ia5String string to afw_utf8_t *.
 * @param to_internal place to return result.  Must be sizeof(afw_utf8_t).
 * @param from_utf8 to convert.
 * @param p to use for returned internal.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_data_type_ia5String_to_internal(
    afw_utf8_t *to_internal,
    const afw_utf8_t *from_utf8,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Convert data type ia5String internal representation to utf-8.
 * @param internal (const afw_utf8_t *) to convert.
 * @param p to use for returned string.
 * @param xctx of caller.
 * @return (const afw_utf8_t *) normalized string representation of value.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_data_type_ia5String_to_utf8(
    const afw_utf8_t * internal,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/** @brief struct for data type ia5String values. */
struct afw_value_ia5String_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal afw_utf8_t value. */
    afw_utf8_t internal;
};

/** @brief struct for managed data type ia5String values.
 *
 * This is the same as afw_value_ia5String_s with the addition of a
 * reference count. This is intended for internal use only.
 */
struct afw_value_ia5String_managed_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal afw_utf8_t value. */
    afw_utf8_t internal;

    /** @brief  Reference count for value. */
    afw_size_t reference_count;

    /** @brief  Pool used to allocate this header (last release frees). */
    const afw_pool_t *p;
};

/** @brief struct for managed slice data type ia5String values.
 *
 * A view into a containing managed value. Holds that value.
 */
struct afw_value_ia5String_managed_slice_s {
    /** @brief  Value inf union with afw_value_t pub to reduce casting needed. */
    union {
        const afw_value_inf_t *inf;
        afw_value_t pub;
    };

    /** @brief  Internal afw_utf8_t view (pointer into containing). */
    afw_utf8_t internal;

    /** @brief  Containing managed value that owns the buffer. */
    const afw_value_ia5String_managed_t *containing_value;

    /** @brief  Reference count for this slice. */
    afw_size_t reference_count;

    /** @brief  Pool used to allocate this slice header. */
    const afw_pool_t *p;
};

/**
 * @brief Typesafe cast of data type ia5String.
 * @param value (const afw_value_t *).
 * @return (const afw_utf8_t *)
 */
AFW_DECLARE(const afw_utf8_t *)
afw_value_as_ia5String(
    const afw_value_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Allocate function for data type ia5String value.
 * @param p to use for returned value.
 * @param xctx of caller.
 * @return Allocated afw_value_ia5String_t with unmanaged inf set.
 *
 * Unmanaged: lifetime is pool p; no value refcount.
 * Caller fills internal after allocate.
 */
AFW_DECLARE(afw_value_ia5String_t *)
afw_value_ia5String_allocate(
    const afw_pool_t *p,
    afw_xctx_t *xctx);
#define afw_value_allocate_unmanaged_ia5String afw_value_ia5String_allocate

/**
 * @brief Create function for managed data type ia5String value.
 * @param internal.
 * @param xctx of caller.
 * @return Created const afw_value_t *.
 *
 * Allocates in p. Starts at reference count 1 (must release).
 * clone_or_reference bumps. Last release frees via stored p.
 * Copies bytes into storage following the header (value owns them).
 */
AFW_DECLARE(const afw_value_t *)
afw_value_ia5String_create_managed(
    const afw_utf8_t * internal,
    const afw_pool_t *p,
    afw_xctx_t *xctx);
#define afw_value_create_managed_ia5String afw_value_ia5String_create_managed

/**
 * @brief Create a managed slice of a managed data type ia5String value.
 * @param containing_value managed (or managed_slice) value with cType afw_utf8_t.
 * @param offset into containing value's internal.
 * @param len of slice.
 * @param xctx of caller.
 * @return Created const afw_value_t * (managed_slice inf).
 *
 * View of a managed string. get_reference on containing. Slice starts
 * at 1 (must release). Header allocated in p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_ia5String_create_managed_slice(
    const afw_value_t *containing_value,
    afw_size_t offset,
    afw_size_t len,
    const afw_pool_t *p,
    afw_xctx_t *xctx);
#define afw_value_create_managed_ia5String_slice afw_value_ia5String_create_managed_slice

/**
 * @brief Create function for unmanaged data type ia5String value.
 * @param internal.
 * @param p to use for returned value.
 * @param xctx of caller.
 * @return Created const afw_value_t *.
 *
 * Allocates in pool p; lifetime is the pool (no value refcount).
 * Copies the utf8/memory header only, not the octets.
 * clone_or_reference creates a managed holdable in p.
 */
AFW_DECLARE(const afw_value_t *)
afw_value_ia5String_create(const afw_utf8_t * internal,
    const afw_pool_t *p, afw_xctx_t *xctx);
#define afw_value_create_unmanaged_ia5String afw_value_ia5String_create

/**
 * @brief Get property function for data type ia5String value.
 * @deprecated
 * @param object of property to get.
 * @param property_name of property to get.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 *
 * This is a deprecated function used to get around an exception that
 * was occurring when an object did not have a pool. Use the function
 * without an "_old" in the name for all new code and replace calls in
 * old code when possible.
 *
 */
#define afw_object_old_get_property_as_ia5String( \
    object, property_name, xctx) \
afw_object_get_property_as_ia5String_source( \
    object, property_name, AFW__FILE_LINE__, \
    ((object)->p ? (object)->p : (xctx)->p), (xctx))

/**
 * @brief Get property function for data type ia5String value.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param p to use for result if evaluation or conversion is required.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 */
#define afw_object_get_property_as_ia5String( \
    object, property_name, p, xctx) \
afw_object_get_property_as_ia5String_source( \
    object, property_name, AFW__FILE_LINE__, p, xctx)

/**
 * @brief Get property function for data type ia5String value.
 * @param object of property to get.
 * @param property_name of property to get.
 * @param source_z file:line.
 * @param p to use for result if evaluation or conversion is required.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_object_get_property_as_ia5String_source(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_z_t *source_z,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Get next property function for data type ia5String value.
 * @deprecated
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 *
 * This is a deprecated function used to get around an exception that
 * was occurring when an object did not have a pool. Use the function
 * without an "_old" in the name for all new code and replace calls in
 * old code when possible.
 *
 */
#define afw_object_old_get_next_property_as_ia5String( \
    object, iterator, property_name, xctx) \
afw_object_get_next_property_as_ia5String_source( \
    object, iterator, property_name, AFW__FILE_LINE__, \
    ((object)->p ? (object)->p : (xctx)->p), (xctx))

/**
 * @brief Get next property function for data type ia5String value.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param p to use for result if evaluation or conversion is required.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 */
#define afw_object_get_next_property_as_ia5String( \
    object, iterator, property_name, p, xctx) \
afw_object_get_next_property_as_ia5String_source( \
    object, iterator, property_name, AFW__FILE_LINE__, p, xctx)

/**
 * @brief Get property function for data type ia5String value.
 * @param object of property to get.
 * @param iterator pointer. Set to NULL before first call.
 * @param property_name is place to return pointer to property name.
 * @param source_z file:line.
 * @param p to use for result if conversion is required.
 * @param xctx of caller.
 * @return const afw_utf8_t *.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_object_get_next_property_as_ia5String_source(
    const afw_object_t *object,
    const afw_iterator_old_t * *iterator,
    const afw_value_t * *property_name,
    const afw_utf8_z_t *source_z,
    const afw_pool_t *p,
    afw_xctx_t *xctx);

/**
 * @brief Set property function for data type ia5String values.
 * @param object of property to set.
 * @param property_name of property to set.
 * @param value of value to set.
 * @param xctx of caller.
 *
 * The value will be allocated in the object's pool.
 * Prefer afw_object_set_property(..., afw_v_*, ...) when a
 * static const value (e.g. from afw_strings.h) already
 * exists for that constant.
 *
 */
AFW_DECLARE(void)
afw_object_set_property_as_ia5String(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_t * internal,
    afw_xctx_t *xctx);

/**
 * @brief Get next value from array of ia5String.
 * @param instance of array.
 * @param iterator.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_utf8_t *) or NULL.
 * 
 * Set the iterator to NULL before the first call and anytime
 * you want to start from the first value again.
 */
#define afw_array_of_ia5String_get_next( \
    array, iterator, xctx) \
    afw_array_of_ia5String_get_next_source( \
    array, iterator, AFW__FILE_LINE__, xctx)

/**
 * @brief Get next value from array of ia5String.
 * @param instance of array.
 * @param iterator.
 * @param source_z file:line.
 * @param xctx of caller.
 * @return (const afw_utf8_t *) or NULL.
 * 
 * Set the iterator to NULL before the first call and anytime
 * you want to start from the first value again.
 */
AFW_DECLARE(const afw_utf8_t *)
afw_array_of_ia5String_get_next_source(
    const afw_array_t *instance,
    const afw_iterator_old_t * *iterator,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx);

/**
 * @brief Add value from array of ia5String.
 * @param instance of array.
 * @param value to add.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_ia5String_add(
    const afw_array_t *instance,
    const afw_utf8_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Remove value from array of ia5String.
 * @param instance of array.
 * @param value to remove.
 * @param xctx of caller.
 */
AFW_DECLARE(void)
afw_array_of_ia5String_remove(
    const afw_array_t *instance,
    const afw_utf8_t *value,
    afw_xctx_t *xctx);

/**
 * @brief Public data type ia5String struct instance.
 *
 * Prefer afw_data_type_ia5String when a pointer is enough and you are not
 * initializing static data that must reference the struct object.
 */
AFW_DECLARE_CONST_DATA(afw_data_type_t)
afw_data_type_ia5String_direct;

/**
 * @brief Public data type ia5String inf.
 *
 * Defined with the data type implementation; managed by the generated
 * data type instance.
 */
AFW_DECLARE_CONST_DATA(afw_data_type_inf_t)
afw_data_type_ia5String_inf;

AFW_END_DECLARES

/** @} */


#endif /* __AFW_DATA_TYPE_IA5STRING_BINDING_H__ */
