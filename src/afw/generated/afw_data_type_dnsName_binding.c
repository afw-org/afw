// See the 'COPYING' file in the project root for licensing information.
/*
 * Adaptive Framework Core Data Types 
 *
 * Copyright (c) 2010-2024 Clemson University
 *
 */

/*  ----------------------------- N O T E -------------------------------------
 *
 * This file is generated by "afwdev generate afw".
 *
 * Do not change this file directly or the changes will be lost the next time
 * this file is regenerated.
 *
 * -------------------------------------------------------------------------*/



/**
 * @file afw_data_type_dnsName_binding.c
 * @brief Generated core adaptive data type implementations.
 */

#include "afw.h"
#include "afw_runtime_object_maps.h"
#include "afw_value_internal.h"

/* Declaration for method optional_release for managed value. */
AFW_DECLARE_STATIC(void)
impl_afw_value_managed_optional_release(
    const afw_value_t *instance,
    afw_xctx_t *xctx);

/* Declaration for method optional_release for unmanaged value. */
AFW_DECLARE_STATIC(void)
impl_afw_value_unmanaged_optional_release(
    const afw_value_t *instance,
    afw_xctx_t *xctx);


/* Declaration for method get_reference for value. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_get_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx);


/* Declaration for method get_reference for managed value. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_managed_get_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx);


/* Declaration for method optional_release for managed slice value. */
AFW_DECLARE_STATIC(void)
impl_afw_value_managed_slice_optional_release(
    const afw_value_t *instance,
    afw_xctx_t *xctx);

/* Declaration for method get_reference for managed slice value. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_managed_slice_get_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx);


/* Declaration for method get_reference for permanent value. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_permanent_get_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx);

/* get_assignable_value with no dest p: bump via get_reference. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_get_assignable_via_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx);


#define impl_afw_value_get_evaluated_meta \
    afw_value_internal_get_evaluated_meta_default

#define impl_afw_value_get_evaluated_metas \
    afw_value_internal_get_evaluated_metas_default

/* This is fully evaluated so optional_evaluate method is NULL. */
#define impl_afw_value_optional_evaluate NULL

/* Inf specific is always data type. */
#define AFW_IMPLEMENTATION_SPECIFIC (const void *)&afw_data_type_dnsName_direct

/* Define inf variables for data_type and is_evaluated_of_data_type. */
#define AFW_IMPLEMENTATION_INF_VARIABLES \
    (const void *)&afw_data_type_dnsName_direct, \
    (const void *)&afw_data_type_dnsName_direct

AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_get_assignable_value(
    const afw_value_t *instance,
    afw_xctx_t *xctx);

/* Declares and rti/inf defines for interface afw_value */
/* unmanaged dnsName: get_reference/release throw; */
/* get_assignable_value creates a managed holdable in xctx->p. */
#define AFW_IMPLEMENTATION_ID "dnsName"
#define AFW_IMPLEMENTATION_INF_SPECIFIER AFW_DEFINE_CONST_DATA
#define AFW_IMPLEMENTATION_INF_LABEL afw_value_unmanaged_dnsName_inf
#define impl_afw_value_optional_release impl_afw_value_unmanaged_optional_release
#define impl_afw_value_get_reference impl_afw_value_get_reference
#define impl_afw_value_get_assignable_value impl_afw_value_get_assignable_value
#define impl_afw_value_create_iterator NULL
#include "afw_value_impl_declares.h"
#undef AFW_IMPLEMENTATION_ID
#undef AFW_IMPLEMENTATION_INF_LABEL
#undef impl_afw_value_optional_release
#undef impl_afw_value_get_reference
#undef impl_afw_value_get_assignable_value

/* Declares and rti/inf defines for interface afw_value */
/* managed dnsName: optional_release drops RC; */
/* get_reference / get_assignable_value bump. */
#define AFW_IMPLEMENTATION_ID "managed_dnsName"
#define AFW_IMPLEMENTATION_INF_LABEL afw_value_managed_dnsName_inf
#define impl_afw_value_optional_release impl_afw_value_managed_optional_release
#define impl_afw_value_get_reference impl_afw_value_managed_get_reference
#define impl_afw_value_get_assignable_value impl_afw_value_get_assignable_via_reference
#define AFW_VALUE_INF_ONLY 1
#include "afw_value_impl_declares.h"
#undef AFW_IMPLEMENTATION_ID
#undef AFW_IMPLEMENTATION_INF_LABEL
#undef impl_afw_value_optional_release
#undef impl_afw_value_get_reference
#undef impl_afw_value_get_assignable_value
#undef AFW_VALUE_INF_ONLY

/* Declares and rti/inf defines for interface afw_value */
/* managed_slice dnsName: own RC; holds containing; */
/* last release frees slice header via xctx->p. */
#define AFW_IMPLEMENTATION_ID "managed_slice_dnsName"
#define AFW_IMPLEMENTATION_INF_LABEL afw_value_managed_slice_dnsName_inf
#define impl_afw_value_optional_release impl_afw_value_managed_slice_optional_release
#define impl_afw_value_get_reference impl_afw_value_managed_slice_get_reference
#define impl_afw_value_get_assignable_value impl_afw_value_get_assignable_via_reference
#define AFW_VALUE_INF_ONLY 1
#include "afw_value_impl_declares.h"
#undef AFW_IMPLEMENTATION_ID
#undef AFW_IMPLEMENTATION_INF_LABEL
#undef impl_afw_value_optional_release
#undef impl_afw_value_get_reference
#undef impl_afw_value_get_assignable_value
#undef AFW_VALUE_INF_ONLY

/* Declares and rti/inf defines for interface afw_value */
/* permanent dnsName: optional_release NULL; */
/* get_reference / get_assignable_value as-is. */
#define AFW_IMPLEMENTATION_ID "permanent_dnsName"
#define AFW_IMPLEMENTATION_INF_LABEL afw_value_permanent_dnsName_inf
#define impl_afw_value_optional_release NULL
#define impl_afw_value_get_reference impl_afw_value_permanent_get_reference
#define impl_afw_value_get_assignable_value impl_afw_value_get_assignable_via_reference
#define AFW_VALUE_INF_ONLY 1
#include "afw_value_impl_declares.h"
#undef AFW_IMPLEMENTATION_ID
#undef AFW_IMPLEMENTATION_INF_LABEL
#undef impl_afw_value_optional_release
#undef impl_afw_value_get_reference
#undef impl_afw_value_get_assignable_value
#undef AFW_VALUE_INF_ONLY

static const afw_value_string_t
impl_data_type_dnsName_id_value = {
    {&afw_value_permanent_string_inf},
    AFW_UTF8_LITERAL("dnsName")
};

AFW_DEFINE_CONST_DATA(afw_value_t *)
afw_data_type_dnsName_id_value =
{
    (const afw_value_t *)&impl_data_type_dnsName_id_value
};

static const afw_utf8_t
impl_data_type_path_dnsName =
    AFW_UTF8_LITERAL("/afw/_AdaptiveDataType_/dnsName");

static const afw_value_object_t
impl_data_type_object_dnsName__value;

/* Data type dnsName object. */
static const afw_runtime_object_indirect_t
impl_data_type_object_dnsName = {
    {
        &afw_runtime_inf__AdaptiveDataType_,
        NULL,
        (const afw_value_t *)&impl_data_type_object_dnsName__value,
        {
            NULL,
            NULL,
            &afw_self_s_dnsName,
            &afw_self_s__AdaptiveDataType_,
            &impl_data_type_path_dnsName
        }
    },
    (void *)&afw_data_type_dnsName_direct
};

static const afw_value_object_t
impl_data_type_object_dnsName__value = {
    {&afw_value_permanent_object_inf},
    (const afw_object_t *)&impl_data_type_object_dnsName
};

/* Permanent empty array of dnsName. */
const afw_array_from_values_self_t
impl_empty_array_of_dnsName;

/* Permanent empty array value of dnsName. */
const afw_value_array_t
impl_value_empty_array_of_dnsName;

/* Data type dnsName instance. */
AFW_DEFINE_CONST_DATA(afw_data_type_t)
afw_data_type_dnsName_direct = {
    &afw_data_type_dnsName_inf,
    (const afw_object_t *)&impl_data_type_object_dnsName,
    AFW_UTF8_LITERAL("dnsName"),
    AFW_UTF8_LITERAL("A DNS name"),
    AFW_UTF8_LITERAL("Data type 'dnsName' is Domain Name System (DNS) name."),
    AFW_UTF8_LITERAL(""),
    afw_data_type_number_dnsName,
    AFW_UTF8_LITERAL("1.3.6.1.4.1.1466.115.121.1.15{64512}"),
    AFW_UTF8_LITERAL("string"),
    AFW_UTF8_LITERAL("idn-hostname"),
    AFW_UTF8_LITERAL("afw_utf8_t"),
    sizeof(afw_utf8_t),
    (const afw_array_t *)&impl_empty_array_of_dnsName,
    (const afw_value_t *)&impl_value_empty_array_of_dnsName,
    &afw_value_unmanaged_dnsName_inf,
    afw_value_clone_dnsName_unmanaged,
    afw_value_clone_dnsName_managed,
    afw_compile_type_error,
    false,
    false,
    false,
    false,
    true,
    false,
    &afw_data_type_string_direct
};

/* Permanent empty array of dnsName. */
const afw_array_from_values_self_t
impl_empty_array_of_dnsName = {
    {
        &afw_array_permanent_from_values_inf,
        NULL,
        (const afw_value_t *)&impl_value_empty_array_of_dnsName
    },
    &afw_data_type_dnsName_direct,
    0,
    NULL
};

/* Permanent empty array value of dnsName. */
const afw_value_array_t
impl_value_empty_array_of_dnsName = {
    {&afw_value_permanent_array_inf},
    (const afw_array_t *)&impl_empty_array_of_dnsName
};

/* Data type struct for dnsName. */
AFW_DEFINE_CONST_DATA(afw_data_type_t *)
afw_data_type_dnsName =
    &afw_data_type_dnsName_direct;

/* Set property from dnsName internal. */
AFW_DEFINE(void)
afw_object_set_property_as_dnsName_internal(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_t * internal,
    afw_xctx_t *xctx)
{
    const afw_value_t *v;

    if (afw_object_is_memory_managed(object) ||
        afw_object_is_memory_wrapper(object)) {
        v = afw_value_dnsName_create_managed(internal, xctx);
    }
    else {
        v = afw_value_dnsName_create(internal, object->p, xctx);
    }
    afw_object_set_property(object, property_name, v, xctx);
}

/* Typesafe cast to evaluated dnsName value. */
AFW_DEFINE(const afw_value_dnsName_t *)
afw_value_as_dnsName(const afw_value_t *value, afw_xctx_t *xctx)
{
    value = afw_value_evaluate(value, xctx->p, xctx);
    if (!AFW_VALUE_IS_DATA_TYPE(value, dnsName))
    {
        const afw_utf8_t *data_type_id;

        if (!value) {
            AFW_THROW_ERROR_Z(general,
                "Typesafe error: expecting 'dnsName' but "
                "encountered 'undefined'",
                xctx);
        }

        data_type_id = afw_value_get_quick_data_type_id(value);
        AFW_THROW_ERROR_FZ(general, xctx,
            "Typesafe error: expecting 'dnsName' but "
            "encountered " AFW_UTF8_FMT_Q ,
            AFW_UTF8_FMT_OPTIONAL_UNDEFINED_ARG(data_type_id));
    }
    return (const afw_value_dnsName_t *)value;
}

/* Typesafe peel of data type dnsName internal. */
AFW_DEFINE(const afw_utf8_t *)
afw_value_as_dnsName_internal(const afw_value_t *value, afw_xctx_t *xctx)
{
    return &afw_value_as_dnsName(value, xctx)->internal;
}

/* Allocate function for data type dnsName values. */
AFW_DEFINE(afw_value_dnsName_t *)
afw_value_dnsName_allocate(const afw_pool_t *p, afw_xctx_t *xctx)
{
    afw_value_dnsName_t *result;

    result = afw_pool_calloc(p, sizeof(afw_value_dnsName_t),
        xctx);
    result->inf = &afw_value_unmanaged_dnsName_inf;
    return result;
}

/* Create function for managed data type dnsName value. */
AFW_DEFINE(const afw_value_t *)
afw_value_dnsName_create_managed(
    const afw_utf8_t * internal,
    afw_xctx_t *xctx)
{
    afw_value_dnsName_managed_t *v;
    afw_size_t len;

    len = (internal) ? internal->len : 0;
    v = afw_pool_calloc(xctx->p,
        sizeof(afw_value_dnsName_managed_t) + len, xctx);
    v->inf = &afw_value_managed_dnsName_inf;
    v->internal.len = len;
    v->internal.s = (const afw_utf8_octet_t *)v +
        sizeof(afw_value_dnsName_managed_t);
    if (internal && internal->s) {
        memcpy((void *)v->internal.s, internal->s, len);
    }
    v->reference_count = 1;

    return &v->pub;
}

/* Create function for managed data type dnsName slice value. */
AFW_DEFINE(const afw_value_t *)
afw_value_dnsName_create_managed_slice(
    const afw_value_t *containing_value,
    afw_size_t offset,
    afw_size_t len,
    afw_xctx_t *xctx)
{
    const afw_value_dnsName_managed_t *containing;
    const afw_utf8_t *base;
    afw_value_dnsName_managed_slice_t *v;

    if (!containing_value) {
        AFW_THROW_ERROR_Z(general,
            "containing_value required for managed slice", xctx);
    }
    if (containing_value->inf == &afw_value_managed_slice_dnsName_inf) {
        const afw_value_dnsName_managed_slice_t *slice =
            (const afw_value_dnsName_managed_slice_t *)containing_value;
        containing = slice->containing_value;
        base = &slice->internal;
    }
    else if (containing_value->inf == &afw_value_managed_dnsName_inf) {
        containing =
            (const afw_value_dnsName_managed_t *)containing_value;
        base = &containing->internal;
    }
    else {
        AFW_THROW_ERROR_Z(general,
            "containing_value must be managed or managed_slice dnsName",
            xctx);
    }
    if (offset > base->len || len > base->len - offset) {
        AFW_THROW_ERROR_Z(general,
            "managed slice offset/len out of range", xctx);
    }
    v = afw_pool_calloc(xctx->p, sizeof(afw_value_dnsName_managed_slice_t), xctx);
    v->inf = &afw_value_managed_slice_dnsName_inf;
    v->internal.s = base->s + offset;
    v->internal.len = len;
    v->containing_value = containing;
    v->reference_count = 1;
    afw_value_add_reference(&containing->pub, xctx);
    return &v->pub;
}

/* Create function for data type dnsName value. */
AFW_DEFINE(const afw_value_t *)
afw_value_dnsName_create(const afw_utf8_t * internal,
    const afw_pool_t *p, afw_xctx_t *xctx)
{
    afw_value_dnsName_t *v;

    v = afw_pool_calloc(p, sizeof(afw_value_dnsName_t),
        xctx);
    v->inf = &afw_value_unmanaged_dnsName_inf;
    if (internal) {
        memcpy(&v->internal, internal, sizeof(afw_utf8_t));
    }
    return &v->pub;
}

/* Deep clone evaluated dnsName unmanaged into p. */
AFW_DEFINE(const afw_value_t *)
afw_value_clone_dnsName_unmanaged(
    const afw_value_t *value,
    const afw_pool_t *p,
    afw_xctx_t *xctx)
{
    afw_value_common_t *cloned;

    if (value->inf == &afw_value_permanent_dnsName_inf) {
        return value;
    }
    cloned = afw_value_common_allocate(
        afw_data_type_dnsName, p, xctx);
    afw_data_type_clone_internal(afw_data_type_dnsName,
        (void *)&cloned->internal,
        (const void *)&((const afw_value_dnsName_t *)value)->internal,
        p, xctx);
    return &cloned->pub;
}

/* Clone evaluated dnsName managed in xctx->p. */
AFW_DEFINE(const afw_value_t *)
afw_value_clone_dnsName_managed(
    const afw_value_t *value,
    afw_xctx_t *xctx)
{
    if (value->inf == &afw_value_permanent_dnsName_inf) {
        return value;
    }
    if (afw_utf8_starts_with_utf8_z(
            &value->inf->rti.implementation_id, "managed_")) {
        return afw_value_get_reference(value, xctx);
    }
    return afw_value_dnsName_create_managed(
        &((const afw_value_dnsName_t *)value)->internal,
        xctx);
}

/* Convert data type dnsName string to afw_utf8_t *. */
AFW_DEFINE(void)
afw_data_type_dnsName_to_internal(afw_utf8_t *to_internal,
    const afw_utf8_t *from_utf8, const afw_pool_t *p, afw_xctx_t *xctx)
{
    afw_data_type_utf8_to_internal(
        &afw_data_type_dnsName_direct,
        (void *)to_internal, from_utf8, p, xctx);
}

/*  Convert data type dnsName internal representation to utf-8. */
AFW_DEFINE(const afw_utf8_t *)
afw_data_type_dnsName_to_utf8(const afw_utf8_t * internal,
    const afw_pool_t *p, afw_xctx_t *xctx)
{
    return afw_data_type_internal_to_utf8(
        &afw_data_type_dnsName_direct,
        internal, p, xctx);
}

/* Get property as dnsName value. */
AFW_DEFINE(const afw_value_dnsName_t *)
afw_object_get_property_as_dnsName_source(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx)
{
    const afw_value_t *value;

    value = afw_object_get_property(object, property_name, xctx);
    if (!value) {
        return NULL;
    }
    if (!AFW_VALUE_IS_DATA_TYPE(value, dnsName)) {
        const afw_utf8_t *data_type_id;

        data_type_id = afw_value_get_quick_data_type_id(value);
        afw_error_set_fz(afw_error_code_general, source_z, xctx,
            "Typesafe error: expecting 'dnsName' but "
            "encountered " AFW_UTF8_FMT_Q,
            AFW_UTF8_FMT_OPTIONAL_UNDEFINED_ARG(data_type_id));
        afw_error_processing_throw((xctx), afw_error_code_general);
    }
    return (const afw_value_dnsName_t *)value;
}

/* Get property function for data type dnsName internal. */
AFW_DEFINE(const afw_utf8_t *)
afw_object_get_property_as_dnsName_internal_source(
    const afw_object_t *object,
    const afw_value_t *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx)
{
    const afw_value_dnsName_t *value;

    value = afw_object_get_property_as_dnsName_source(object, property_name, source_z, xctx);
    if (!value) {
        return NULL;
    }
    return &value->internal;
}

/* Get next property as dnsName value. */
AFW_DEFINE(const afw_value_dnsName_t *)
afw_object_get_next_property_as_dnsName_source(
    const afw_object_t *object,
    const afw_iterator_old_t * *iterator,
    const afw_value_t * *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx)
{
    const afw_value_t *value;

    value = afw_object_get_next_property(object, iterator, property_name, xctx);
    if (!value) {
        return NULL;
    }
    if (!AFW_VALUE_IS_DATA_TYPE(value, dnsName)) {
        const afw_utf8_t *data_type_id;

        data_type_id = afw_value_get_quick_data_type_id(value);
        afw_error_set_fz(afw_error_code_general, source_z, xctx,
            "Typesafe error: expecting 'dnsName' but "
            "encountered " AFW_UTF8_FMT_Q,
            AFW_UTF8_FMT_OPTIONAL_UNDEFINED_ARG(data_type_id));
        afw_error_processing_throw((xctx), afw_error_code_general);
    }
    return (const afw_value_dnsName_t *)value;
}

/* Get next property function for data type dnsName internal. */
AFW_DEFINE(const afw_utf8_t *)
afw_object_get_next_property_as_dnsName_internal_source(
    const afw_object_t *object,
    const afw_iterator_old_t * *iterator,
    const afw_value_t * *property_name,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx)
{
    const afw_value_dnsName_t *value;

    value = afw_object_get_next_property_as_dnsName_source(object, iterator, property_name, source_z, xctx);
    if (!value) {
        return NULL;
    }
    return &value->internal;
}

/* Implementation of method optional_release for managed value. */
AFW_DECLARE_STATIC(void)
impl_afw_value_managed_optional_release(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    afw_value_dnsName_managed_t *self =
        (afw_value_dnsName_managed_t *)instance;

    if (self->reference_count == 0) {
        return;
    }
    self->reference_count--;
    if (self->reference_count == 0) {
        afw_pool_free_memory(xctx->p, self,
            sizeof(afw_value_dnsName_managed_t) + self->internal.len, xctx);
    }
}

/* Implementation of method optional_release for unmanaged value. */
AFW_DECLARE_STATIC(void)
impl_afw_value_unmanaged_optional_release(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    (void)instance;
    AFW_THROW_ERROR_Z(general,
        "release of unmanaged scalar", xctx);
}

/* Implementation of method get_reference for unmanaged value. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_get_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    (void)instance;
    AFW_THROW_ERROR_Z(general,
        "get_reference of unmanaged scalar", xctx);
}

/* Slot fill: promote to managed in xctx->p. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_get_assignable_value(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    const afw_value_dnsName_t *self =
        (const afw_value_dnsName_t *)instance;

    return afw_value_dnsName_create_managed(
        &self->internal, xctx);
}


/* Implementation of method get_reference for managed value. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_managed_get_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    afw_value_dnsName_managed_t *self =
        (afw_value_dnsName_managed_t *)instance;

    (void)xctx;
    self->reference_count++;
    return instance;
}


/* Implementation of method optional_release for managed slice. */
AFW_DECLARE_STATIC(void)
impl_afw_value_managed_slice_optional_release(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    afw_value_dnsName_managed_slice_t *self =
        (afw_value_dnsName_managed_slice_t *)instance;

    if (self->reference_count == 0) {
        return;
    }
    self->reference_count--;
    if (self->reference_count == 0) {
        if (self->containing_value) {
            afw_value_release(&self->containing_value->pub, xctx);
        }
        afw_pool_free_memory(xctx->p, self,
            sizeof(afw_value_dnsName_managed_slice_t), xctx);
    }
}

/* Implementation of method get_reference for managed slice. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_managed_slice_get_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    afw_value_dnsName_managed_slice_t *self =
        (afw_value_dnsName_managed_slice_t *)instance;

    (void)xctx;
    self->reference_count++;
    return instance;
}


/* Implementation of method get_reference for permanent value. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_permanent_get_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    /* Permanent scalar: same instance as-is. */
    (void)xctx;
    return instance;
}

/* get_assignable_value: no dest p; bump via inf get_reference. */
AFW_DECLARE_STATIC(const afw_value_t *)
impl_afw_value_get_assignable_via_reference(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    return afw_value_get_reference(instance, xctx);
}

/*
 * Implementation of method get_data_type for interface afw_value.
 */
const afw_data_type_t *
impl_afw_value_get_data_type(
    const afw_value_t *instance,
    afw_xctx_t *xctx)
{
    return afw_data_type_dnsName;
}

/*
 * Implementation of method compiler_listing for interface afw_value.
 */
void
impl_afw_value_produce_compiler_listing(
    const afw_value_t *instance,
    const afw_writer_t *writer,
    afw_xctx_t *xctx)
{
    afw_data_type_value_compiler_listing(
        afw_data_type_dnsName,
        writer, instance, xctx);
}

/*
 * Implementation of method decompile for interface afw_value.
 */
void
impl_afw_value_decompile(
    const afw_value_t *instance,
    const afw_writer_t *writer,
    afw_xctx_t *xctx)
{
    afw_data_type_write_as_expression(
        afw_data_type_dnsName,
        writer,
        (const void *)&(((const afw_value_common_t *)instance)->internal),
        xctx);
}

/*
 * Implementation of method get_info for interface afw_value.
 */
void
impl_afw_value_get_info(
    const afw_value_t *instance,
    afw_value_info_t *info,
    const afw_pool_t *p,
    afw_xctx_t *xctx)
{
    afw_memory_clear(info);
    info->value_inf_id = &instance->inf->rti.implementation_id;
    info->evaluated_data_type = afw_data_type_dnsName;
    info->optimized_value = instance;
}


/* Get next dnsName value from array of dnsName. */
AFW_DEFINE(const afw_value_dnsName_t *)
afw_array_of_dnsName_get_next_source(
    const afw_array_t *instance,
    const afw_iterator_old_t * *iterator,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx)
{
    const afw_value_t *value;

    value = afw_array_get_next_value(instance, iterator, NULL, xctx);
    if (!value) {
        return NULL;
    }
    if (!AFW_VALUE_IS_DATA_TYPE(value, dnsName)) {
        const afw_utf8_t *data_type_id;

        data_type_id = afw_value_get_quick_data_type_id(value);
        afw_error_set_fz(afw_error_code_general, source_z, xctx,
            "Typesafe error: expecting 'dnsName' but "
            "encountered " AFW_UTF8_FMT_Q,
            AFW_UTF8_FMT_OPTIONAL_UNDEFINED_ARG(data_type_id));
        afw_error_processing_throw((xctx), afw_error_code_general);
    }
    return (const afw_value_dnsName_t *)value;
}

/* Get next dnsName internal from array of dnsName. */
AFW_DEFINE(const afw_utf8_t *)
afw_array_of_dnsName_get_next_internal_source(
    const afw_array_t *instance,
    const afw_iterator_old_t * *iterator,
    const afw_utf8_z_t *source_z,
    afw_xctx_t *xctx)
{
    const afw_value_dnsName_t *value;

    value = afw_array_of_dnsName_get_next_source(instance, iterator, source_z, xctx);
    if (!value) {
        return NULL;
    }
    return &value->internal;
}

/* Add a dnsName value to array of dnsName. */
AFW_DEFINE(void)
afw_array_of_dnsName_add(
    const afw_array_t *instance,
    const afw_value_dnsName_t *value,
    afw_xctx_t *xctx)
{
    const afw_array_setter_t *setter;

    setter = afw_array_get_setter(instance, xctx);
    if (!setter) {
        AFW_LIST_ERROR_OBJECT_IMMUTABLE;
    }
    afw_array_setter_push_value(setter, &value->pub, xctx);
}

/* Add a dnsName internal to array of dnsName. */
AFW_DEFINE(void)
afw_array_of_dnsName_add_internal(
    const afw_array_t *instance,
    const afw_utf8_t *value,
    afw_xctx_t *xctx)
{
    const afw_value_t *v;

    if (afw_array_is_memory_managed(instance) ||
        afw_array_is_memory_wrapper(instance)) {
        v = afw_value_dnsName_create_managed(value, xctx);
    }
    else {
        v = afw_value_dnsName_create(value, instance->p, xctx);
    }
    afw_array_push_value(instance, v, xctx);
}

/* Remove a dnsName value from array of dnsName. */
AFW_DEFINE(void)
afw_array_of_dnsName_remove(
    const afw_array_t *instance,
    const afw_value_dnsName_t *value,
    afw_xctx_t *xctx)
{
    afw_array_of_dnsName_remove_internal(instance, &value->internal, xctx);
}

/* Remove a dnsName internal from array of dnsName. */
AFW_DEFINE(void)
afw_array_of_dnsName_remove_internal(
    const afw_array_t *instance,
    const afw_utf8_t *value,
    afw_xctx_t *xctx)
{
    const afw_value_t *v;

    v = afw_value_dnsName_create(value, xctx->p, xctx);
    afw_array_remove_value(instance, v, xctx);
}
