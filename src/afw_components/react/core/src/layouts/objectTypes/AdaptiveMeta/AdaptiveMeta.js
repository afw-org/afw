// See the 'COPYING' file in the project root for licensing information.
import {ObjectResponsive} from "../ObjectResponsive";

export const AdaptiveMeta = (props) => {

    return (
        <ObjectResponsive 
            {...props}
        />
    );
};

export default AdaptiveMeta;
