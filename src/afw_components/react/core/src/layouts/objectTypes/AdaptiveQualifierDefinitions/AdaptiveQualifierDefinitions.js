// See the 'COPYING' file in the project root for licensing information.
import {ObjectResponsive} from "../ObjectResponsive";

export const AdaptiveQualifierDefinitions = (props) => {

    return (
        <ObjectResponsive 
            {...props}
        />
    );
};

export default AdaptiveQualifierDefinitions;
