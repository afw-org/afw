import {Divider} from "./Divider";

export default {
    title: "core / content / Divider",
    component: Divider,
    argTypes: {
    }
};


const Template = (args) => <Divider {...args} />;

export const Default = Template.bind({});

export const AllProps = Template.bind({});
Divider.args = {
};
