#!/usr/bin/env python3

# afw_yaml content-type tests:
#   - yaml_allow_output.py  — --allow serialization, conf -t yaml (issue #14)
#   - yaml_to_object.py     — raw_to_object / file adapter yaml / #17 faces
Tags = ["yaml", "content_type"]
